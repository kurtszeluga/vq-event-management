# VQ Event Management Upgrade Plan

This document tracks the security, reliability, usability, and product improvements identified during the July 2026 site review. Update the status and completion log whenever an item is finished so the remaining work stays visible.

## Status Key

- `Not Started`
- `In Progress`
- `Completed`
- `Deferred`

## Phase 1 - Registration Security

| Item | Status | Notes |
| --- | --- | --- |
| Stop returning member phone and billing address before identity verification | Completed | Public lookup returns only profile existence and the required verification step. Contact, membership, and billing details are returned only after verification. |
| Require Firebase authentication for signed-in/password users | Completed | The server verifies the Firebase ID token, registration email, and linked profile UID. |
| Replace phone-number fallback with a Resend one-time email code | Completed | Six-digit codes expire after 10 minutes, allow five attempts, limit sends, and are stored only as hashes. |
| Issue a short-lived, one-use registration verification token | Completed | Email verification issues a hashed, event-and-email-bound token that expires after 20 minutes and is consumed during registration. |
| Require verified identity in the registration API | Completed | Both member and permitted non-member registrations now require Firebase authentication or a valid email-verification token. |
| Deny direct browser creation of Firestore registration records | Completed | Firestore denies all client registration creates; the Firebase Admin API is the sole creation path. |
| Remove the obsolete public phone-verification endpoint | Completed | Removed the endpoint and client calls. After Phase 2 added `/api/square-webhook`, the Vercel API count is 12 (Hobby plan limit). |
| Add focused automated checks for registration verification | Completed | Five Node tests, changed-file lint, production build, rules compilation, and a direct-write denial smoke check pass. |

### Phase 1 Production Checklist

- [x] Push the application changes and confirm the Vercel deployment succeeds.
- [x] Publish `firestore.rules` to Firebase before testing registration.
- [x] Deploy the Firestore indexes; `firestore.indexes.json` now enables TTL for `registrationVerifications.expiresAt` so expired verification records are cleaned up automatically.
- [x] Test a signed-in member registration.
- [x] Test a profile/password registration and the email-code fallback.
- [x] Test a CSV-created profile that has no activated login.
- [x] Test an allowed non-member registration and a blocked non-member registration.
- [x] Confirm profile contact and billing information never appears before successful verification.
- [x] Confirm incorrect, expired, and reused verification codes/tokens are rejected.

## Phase 2 - Payment And Capacity Reliability

| Item | Status | Notes |
| --- | --- | --- |
| Add temporary seat reservations during online checkout | Completed | Online Square checkout now creates a private 5-minute event/email-bound reservation before tokenizing the card, counts active holds against capacity, and consumes the hold when the registration is written. |
| Add Square webhook signature verification | Completed | Added `/api/square-webhook`, HMAC signature verification, private `squareWebhookEvents` logging, and conservative payment/refund reconciliation hooks. |
| Add payment reconciliation tools | Completed | Added an admin Payment Review module and Needs Attention count for Square webhook events requiring review. |
| Initiate Square refunds from the app | Completed | Added the System Config toggle and guarded online Square refund action. Pending Square refunds cancel the registration immediately, return the seat, create Payment Review follow-up, send a registrant notification, and reconcile when Square webhook completion arrives. |
| Add payment and card-testing rate limits | Completed | Added Firestore-backed API rate limits for registration lookup, email-code sends, code verification, Square seat holds, registration submits, membership confirmation sends, admin payment updates, and refund requests. `apiRateLimits.expiresAt` is TTL-managed. |
| Enforce idempotency across registration retries | Completed | Registration submit attempts now carry a stable browser-generated attempt key, store a private `registrationAttempts` record, reuse existing results on retry, and send the same key to Square to avoid duplicate charges. |

## Phase 3 - Data Security And Permissions

| Item | Status | Notes |
| --- | --- | --- |
| Create a directory-safe member collection | Completed | Added `memberDirectoryProfiles` projection with sync on profile/membership writes, directory query switched off `users`, and Firestore rules that remove peer reads of full user documents. Run `npm run backfill:member-directory` after deploying rules. |
| Correct and test member-directory Firestore queries | Completed | Member directory query now includes both `status == Active` and `membershipStatus == Active`, matching Firestore rule requirements. |
| Restrict event file uploads to authorized event administrators | Completed | Storage rules now require an active Super User or Admin with `manageEvents`, and uploads remain limited to the signed-in user's own folder. |
| Route sensitive writes through authenticated server endpoints | In Progress | Event create/update/archive/reactivate/delete and admin password changes now go through authenticated Admin APIs; client event writes are denied. Remaining: membership CSV/import and some configuration writes are still client-side. |
| Add API rate limiting and abuse monitoring | Completed | Extended Firestore-backed rate limits to `file-proxy`, public feeds/counts, supply-list viewer, admin user create/update, and event management. Monitoring/alerts remain a later ops item. |
| Add production security headers | Completed | Added CSP, frame protection, referrer policy, content-type protection, and permissions policy via `vercel.json`. |
| Add a privacy policy and support/contact page | Completed | Added `/privacy` and `/support` pages plus footer links. |

## Phase 4 - Event And Registration Workflows

| Item | Status | Notes |
| --- | --- | --- |
| Enforce registration opening and closing dates automatically | Completed | Availability is derived from `registrationOpenAt`/`registrationCloseAt` by `shared/registrationWindow.js`, used by the register-page gate, `/api/create-registration`, the public feed, and all four listing views. The stored `registrationOpen` boolean is no longer read anywhere. Naive `datetime-local` values are resolved in `America/New_York` on both sides, because the Vercel runtime is UTC. `EventForm` now rejects a close date at or before the open date. Confirmed working in production after deploy: an event's status changed to Registration Closed when its configured time lapsed, which also validates the guild-local timezone resolution against the UTC Vercel runtime. Note: existing events whose windows already passed correctly read as closed now and need their dates corrected if they are meant to be open. |
| Cascade event archiving to its registrations, unless payment is still pending | Completed | Archiving an event now moves its registrations out of the admin Registrations card's default view together with it, rather than leaving them listed against an event that no longer shows up for editing. `api/admin-manage-event.js`'s `archive` action queries `registrations` by `eventId` and refuses (409) via a new pure `getArchiveBlockError()` (`api/_lib/registration-capacity.js`) whenever any registration is still payment-pending per the existing `isPaymentPending()` predicate - Waitlisted registrants never block, since they hold no seat and owe nothing. That predicate (and `isCashCheckAwaitingCollection()`) moved to a new dependency-free `shared/registrationPayment.js` so the server check and the client util share one definition, following the `shared/registrationWindow.js` precedent; `src/utils/registrationEligibility.js` re-exports both so no existing import broke. Found and fixed a real bug while wiring this up: `AdminDashboardPage.jsx`'s archive-confirm handler had no `catch` at all, so a refusal would have surfaced only as a silent unhandled promise rejection; added an `error` prop to the shared `ConfirmDialog` and a `catch` that displays the server's message and keeps the dialog open. `RegistrationPanel.jsx` gained an Active/Archived event-status filter (mirroring `EventList.jsx`'s existing one), defaulting to Active - so nothing is deleted and the full history stays reachable via the toggle, it just no longer clutters the live view. 9 new/extended tests (6 server for `getArchiveBlockError`, 2 client for the dashboard error surfacing, 3 client for the Registrations-card filter); full suite green (134/134 server, 14/14 rules, 173/173 client), lint and build clean. Live browser verification not performed in this environment. |
| Add secure guest Manage My Registration links | Not Started | Allow guests to review or cancel without creating an account. |
| Add automatic waitlist promotion | Completed | When a seat frees up (cancellation or refund), the earliest eligible Waitlisted registrant is emailed an offer to claim it themselves via a magic-link claim page - no auto-registration or auto-charging. `isSeatHoldingRegistration()` (`api/_lib/registration-capacity.js`) now also counts a Waitlisted registration with an unexpired offer, which is the entire "hold the seat during the claim window" mechanism; no new reservation collection. The claim window is 3 days rather than a tighter figure because Vercel's Hobby plan limits cron jobs to once a day, and a shorter nominal window couldn't be enforced reliably at that check frequency. If the waitlist is empty when a seat opens, the coordinator gets a one-time notification instead of per-offer noise. Admins get a "Promote" action (new `manageWaitlist` permission) plus waitlist rank/offer-status visibility in `RegistrationPanel.jsx`. New logic lives in `api/_lib/waitlist.js`; the claim and daily cron actions were added to the existing `create-registration.js` action router rather than a new file, keeping the Hobby-plan function count at 12. See the matching Completion Log entries for the full design rationale and a real bug caught while extending capacity logic. |
| Add calendar files and reminder emails | Not Started | Include add-to-calendar, one-week, one-day, cancellation, and location-change notices. |
| Add Duplicate Event and reusable event templates | Not Started | Reduce data entry for recurring programs and workshops. |
| Allow up to 4 photos per listing, flippable one-at-a-time in the existing image slot | Completed | The data model and admin upload flow (`EventForm.jsx`) already had full multi-image plumbing - `handleImageUrl`/`handleFileUpload`/`handleRemoveImage` were already index-based - but the render loop was hardcoded to `[0].map(...)` (one upload slot) and both the save path (`.slice(0, 1)`) and the edit-load path (`imageUrls?.[0]` only) explicitly capped it to one image; raising the cap was a bounded, low-risk change rather than new plumbing. New shared `MAX_EVENT_IMAGES` constant (`shared/eventImages.js`, imported by both the client form and the server) replaces that hardcoded `1` everywhere: 4 upload slots in `EventForm.jsx`, a matching 4-slot load when editing an existing event (only ever reading the first `MAX_EVENT_IMAGES`, so legacy events with more stored data still open cleanly), and a new `getEventImagesError()` in `api/_lib/registration-capacity.js` (alongside the existing `getEventCapacityError()`) as a defense-in-depth 400 on this authenticated write path, since the client already enforces the cap before sending. Confirmed the display design directly with the user first - one photo visible at a time with prev/next arrows, not a multi-image strip - before writing any code. New `src/components/EventImageCarousel.jsx` renders exactly like the old single-image-or-placeholder ternary when there are 0 or 1 images (verified live against the real dev database: existing single-image events on `/events` and `/business-listings` render pixel-identical to before, no console errors), and only shows CSS-drawn chevron arrows plus dot indicators once there are 2+ photos - it never shows more than one at a time. Wired into the three public card locations that actually needed it (`EventsPage.jsx`, `EventDetailsPage.jsx`, `PublicListingPage.jsx`, the last shared by Business Listings and For Sale); deliberately left untouched: the admin `EventList.jsx` thumbnail (management view, not a member-facing listing card), `EventListingPrintPage.jsx` and the raw-HTML print builder in `EventsPage.jsx` (static print output), and the GoDaddy embed feed (`api/_lib/public-event-feed.js`, a third-party widget outside this app's control) - all still show the first photo only, which is unchanged behavior for them. `buildEventPayload()`/`getInitialForm()` exported from `EventForm.jsx` for direct testing (2 pre-existing-pattern lint warnings, not errors - matches this codebase's established `getProfileStatus`-style "exported for testing" precedent; extracting them to a separate file to silence a non-blocking warning was judged disproportionate to this task). 14 new client tests (7 for the carousel: placeholder/single-image/blank-slot-filtering/prev-next-wraparound/active-dot cases; 7 for the 4-slot save/load logic) and 2 new server tests for `getEventImagesError`; full suite green (142/142 server, 19/19 rules, 210/210 client), lint clean aside from the two accepted warnings, build clean. |
| Add coordinator-area permissions | Not Started | Coordinators should manage only their assigned areas when appropriate. |
| Add attendance and roster tools | Not Started | Include printable rosters, CSV export, and optional check-in. |

## Phase 5 - Admin Operations

| Item | Status | Notes |
| --- | --- | --- |
| Build a unified Needs Attention queue | Not Started | Pending memberships, unpaid registrations, failed payments, waitlists, email bounces, and incomplete upcoming events. |
| Add an admin activity-log viewer | Not Started | Audit records already exist but are not visible in the application. |
| Add operational and financial reports | Not Started | Event totals, outstanding payments, cash/check deposits, Square totals, refunds, attendance, and membership renewals. |
| Add Resend delivery and bounce tracking | Not Started | Display failures and suppress repeatedly invalid addresses. |
| Add CSV import preview and rollback support | In Progress | Uploading a CSV now parses and previews it entirely client-side (`analyzeMemberCsv()`) - row counts, skipped-row and duplicate-email warnings by row number - with an explicit Import confirmation step before anything reaches Firestore; zero valid rows blocks the import outright. The chunked/batched writes underneath are unchanged. Not done: no transactional rollback if a batch mid-sequence fails partway through a large import (lower risk now that the directory-sync budget bug below is fixed, but not eliminated). |
| Add scheduled Firestore backups and a recovery procedure | Not Started | Document restore steps and test them periodically. |
| Let a specific admin register a guild member for an event on their behalf | Completed | Added an individually-assignable `registerOthers` permission (User Controls) and a "Register a Member" action in the Registrations card for admins who hold it, for guild members who don't use or are afraid of online tools. Cash/check payment only - no card entry in this flow. Requires `firebase deploy --only firestore:rules` before it takes effect in production, and `registerOthers` must be granted to a real account via User Controls before the manual verification checklist can be run end-to-end. See the matching Completion Log entry for the full wiring and testing detail. |
| Add an Archive module to view, print, and un-archive past events | Completed | New "Archive" tab in Manage/Edit (`src/components/admin/ArchivePanel.jsx`), gated by `manageEvents` the same as the other event tabs. Lists every archived event regardless of type, drills into one for a read-only registrant table, and prints a report (registrant list + a live Total Paid figure) via the same no-route `window.open` + `document.write` pattern `EventsPage.jsx`'s public listing print already uses - no new route or re-fetch, since the panel already has the live registration data on hand. Un-archive reuses the exact `handleDelete`/`ConfirmDialog` flow `EventList.jsx` already had for archive/reactivate; since archiving never deletes registration data, un-archiving needed no new plumbing to bring registrations back into the normal Registrations card. Degrades gracefully for an admin who can manage events but lacks `viewRegistrations`: the tab still shows and Un-archive still works, just without the registrant table or dollar figures. A new pure `getTotalPaidAmount()` (`src/utils/registrationFinancials.js`) sums `amountPaid` only for `paymentStatus === 'Paid'` registrations - which already nets out Refunded/Refund Pending, since `amountPaid` is kept as history after a refund but `paymentStatus` moves off `'Paid'`. The same figure now also appears as a "Total Paid" stat pill on paid events in the Manage Events list (`EventList.jsx`, admin-only - `AdminDashboardPage.jsx` only passes it when the signed-in admin has `viewRegistrations`) and on the Registrations card's own event summary cards (`RegistrationPanel.jsx`) - never on any public listing. Consolidated `AdminDashboardPage.jsx`'s Payment Review badge subscription (previously counting cash/check-awaiting registrations only) into one `registrations` subscription that now also derives `registrationsByEventId` and `totalPaidByEventId`, so the dashboard reads the `registrations` collection once instead of once per consumer. 6 new server tests for `getTotalPaidAmount`, 6 new client tests for `ArchivePanel` (archived-only listing, empty state, drill-in with a waitlisted registrant excluded from the total, permission degradation, un-archive, print), 3 for the `EventList` Total Paid pill, 1 for the `RegistrationPanel` pill, and a 1-line fix to the existing admin-module-nav test's expected module order; full suite green (140/140 server, 14/14 rules, 183/183 client), lint and build clean. Live browser verification not performed in this environment. |
| Deep-link registration management from the event card | Completed | On request, added "Review/Edit Registrations" and "Register A Member" shortcuts to each event card (Events/Activities, Challenges, Business Listings, and For Sale all share `EventList.jsx`) that jump straight into the Registrations module with that event already selected, instead of making the admin find it there again. Pure lifted-state wiring in `AdminDashboardPage.jsx` (`registrationsJump`), mirroring the same "initial filter" prop pattern already used for `UserControlPanel`; `RegistrationPanel.jsx` reproduces its own existing `handleSelectEvent` + `setRegisterMemberOpen` pairing via a new `initialEventId`/`initialRegisterMemberOpen` effect. The self-registration link was relabeled "Register Myself" and moved to the end of the row, left of Archive, now that "Register A Member" exists alongside it. A fast follow-up on request: "Return" from inside that jumped-to event now goes back to whichever module launched it (tracked as `sourceModule`) rather than into the Registrations module's own event picker, falling back to the normal in-module behavior once the admin changes a filter or manually selects a different event. The standalone Registrations module itself is intentionally unchanged - these are shortcuts into it, not a replacement, since it still holds cross-event filtering/history that doesn't belong on one event card. |

## Phase 6 - Member Experience And Accessibility

| Item | Status | Notes |
| --- | --- | --- |
| Add compact mobile navigation | Completed | Two parts. The admin dashboard `Manage/Edit` row collapses behind a single toggle below 760px (464px of stacked buttons down to 48px). The sticky `.site-header` nav now collapses too: below 760px the destinations move into a slide-over sheet (`src/components/MobileNavSheet.jsx`) behind a menu button, and the separate `auth-banner` strip folds into the sheet's account card. Measured on a 375x812 viewport: persistent chrome went from 364px (45% of the viewport) to 98px (12%). Desktop is unchanged at both breakpoints. |
| Increase small labels and helper text | Completed | `b789c8a` raised ~35 font-size declarations toward a 16px floor, leaving only compact visual tags (event type chips, status pills, uppercase category labels) below it. Verified now: `styles.css` has zero declarations under 14px anywhere, and no form control carries a small-font override, so it inherits the 16px body base. This row was left `Not Started` after that commit landed; corrected here. |
| Add consistent keyboard focus and dialog focus management | Completed | `MobileNavSheet` established the pattern first. A follow-up pass extracted that behavior into shared `ModalDialog` / `ConfirmDialog` primitives and applied them to the registration edit modal plus every admin confirmation flow that had still been using native `window.confirm` (`AdminDashboardPage`, `EventForm`, `UserControlPanel`, `ConfigurationPanel`, `RegistrationPanel`). This pass closed the two remaining gaps. First, the supply-list viewer: it opens as a real popup window (`window.open(..., 'popup,...')`), not an in-page overlay, so `ModalDialog`'s Tab-trap doesn't apply - it's a separate browsing context the OS already manages Tab within. What it lacked was dialog-equivalent behavior: focus now moves to the document heading (`tabIndex={-1}` + `.focus()`) once the event loads, Escape closes it (reusing the same close logic as the Close button - `window.close()` when opened as a popup, otherwise history-back or navigate), and the two call sites (`EventDetailsPage`, `EventsPage`) now pass the triggering button through a new `src/utils/popupWindow.js` helper that polls `popup.closed` and restores focus to that button once the popup closes - mirroring the "restore focus to the triggering control" rule the in-page dialogs already followed. Second, focus-visible styling: audited the stylesheet and found exactly one consistent pattern already in use (`outline: 3px solid #cfe3de` on form inputs and the registration event card), and no global rule at all for plain buttons/links, which fell back to inconsistent browser defaults. Added one global `:focus-visible { outline: 3px solid #cfe3de; outline-offset: 2px; }` rule reusing that same established color rather than inventing a second one, so every interactive element site-wide gets a consistent, deliberate keyboard-focus ring that stays silent on mouse clicks. Added 4 tests (client suite 115 to 119: 2 for `SupplyListViewerPage`'s focus-on-load/Escape behavior, 2 for the `popupWindow` polling helper); full suite passes. Mutation-tested: removing either `SupplyListViewerPage` effect fails its matching test. |
| Make directory email and phone values clickable | Completed | `MemberDirectoryPage` now renders `mailto:`/`tel:` links, matching the existing pattern in `PublicListingPage.jsx` and the `EventsPage.jsx` coordinator contact - plain anchors relying on the browser's default underline, since nothing in this app strips it from inline links. The phone link's href is built from the already-formatted display value's digits, so it can't disagree with what's shown. Preserves the existing "Not listed" fallback for a missing value or one with no extractable digits. 5 tests (client 105 to 110), mutation-tested: a missing `mailto:`/`tel:` prefix and a dropped empty-phone guard each fail one test. Verified live against the real member directory (28 active members) once Chrome reconnected: correct `mailto:`/`tel:` hrefs with digits-only phone numbers on every card, formatted display text intact, no console errors. No member in this dataset was missing a value, so the "Not listed" fallback is proven by the component tests rather than live data. |
| Replace raw Firebase errors with plain-language messages | Not Started | Apply across login, profile, registration, and admin screens. |
| Add directory print/export support | Not Started | Support the Guild's current member-directory distribution workflow. |
| Show a member's own full profile detail (status, permissions, dates, payments) | Completed | `/profile`'s read-only view only ever showed Name/Email/Phone/Billing Address; a signed-in member had no way to see their own Role, Account Status, Membership Status, membership payment status, or key dates. Added all of it to the same `dl`, plus a "My Registrations & Payments" link to the existing `/my-registrations` page rather than duplicating that page's registration/payment history here. `getDisplayAccountStatus()`/`getDisplayMembershipStatus()` mirror the Super-User-is-always-Active-and-not-a-tracked-member exceptions already established in `AdminRegisterMemberPanel.jsx`/`UserControlPanel.jsx`/`create-registration.js`'s `getProfileStatus()` - this codebase already carries that logic as several independent copies rather than one shared helper, so a fourth local copy here follows the existing pattern rather than forcing an unrelated consolidation. Individual admin permissions were already listed (comma-joined) in the existing role-gated `status-panel` box above the detail list; left that alone since it already does what "list the individual permissions" asked for. Deliberately did not add a full multi-entry *membership* payment history (as opposed to the single current-payment snapshot, which is already a field on the member's own `users` document and therefore already safely readable): `firestore.rules`' `payments` collection currently allows a signed-in non-admin to read only `entityType == 'Registration'` docs they own, not `'Membership'` ones, so a full history would need a new rule - flagged rather than silently opened. 4 new client tests (full detail rendering, defaults when unset, the Super User exceptions, and the registrations link's href); full suite green (140/140 server, 14/14 rules, 194/194 client), lint and build clean. Live browser verification not performed in this environment. |
| Update favicon and PWA icons to the supplied Guild logo | Not Started | Current installed-app icons still use the old VQ Events artwork. |
| Add self-service forgot-username/password account recovery | Completed | The login page's "Forgot password or username?" panel went through two iterations on request. First: a phone-lookup option that emailed a 6-digit code and, once verified, revealed the account's email address (reusing the registration-verification email-code pattern). Second, on further request: fully collapsed into a single email-or-phone field, a matching code, and - once verified - a real signed-in session (a Firebase custom token hand-signed with `node:crypto`, matching the existing `firebase-token.js` verification approach rather than importing `firebase-admin/auth`, which broke Vercel's function bundling and crashed the endpoint in production the first time it shipped) that lands the member on `/profile` with the form already in edit mode and the password field focused. Email/password fields hide entirely while the recovery panel is open, so there is only ever one thing on screen to fill in - built for members with limited computer experience. Both the phone-recovery action and the account-recovery/waitlist-claim actions live inside the existing `registration-lookup.js`/`create-registration.js` action routers rather than new files, keeping the Hobby-plan function count at 12. See the matching Completion Log entries for the crash postmortem and the exact design tradeoffs (anti-enumeration response shape, token hashing, why the flow grants a full session rather than just a password reset). |

## Phase 7 - Engineering And Deployment

| Item | Status | Notes |
| --- | --- | --- |
| Add automated Firestore rules and workflow tests | In Progress | Capacity, seat holds, payment-hold validation, and Square refund-side webhook detection are covered by 19 tests in `tests/registration-capacity.test.js` and `tests/square-reconciliation.test.js`. Client-side hook and component tests exist in `tests/client/` (vitest). Firestore rules tests now exist too: `tests/rules/users-self-update.test.js` runs against the emulator via `npm run test:rules` and in CI, covering the `users` self-update path - they are what caught the rule exceeding Firestore's 1000-expression evaluation ceiling. `tests/rules/member-directory-batch-create.test.js` adds coverage for `memberDirectoryProfiles` writes at CSV-import scale - what caught the per-request `get()`/`exists()` budget bug. Remaining: rules coverage for the other collections and membership eligibility. |
| Add continuous integration for build, lint, and tests | Completed | `.github/workflows/ci.yml` runs lint, build, server tests, client tests, and the Firestore rules suite (with a JDK step for the emulator) on push to `main` and all pull requests. Matrix is `[24.x]`, matching the Vercel runtime declared in `engines.node`; `fail-fast: false` so one entry cannot mask another. |
| Fix current source lint errors and ignore generated `.vercel` files | Completed | Added `.vercel` to ESLint ignores, fixed the four real source errors, and resolved the `EventForm` exhaustive-deps warning. `npm run lint` is clean. |
| Resolve dependency audit findings | In Progress | `npm audit fix` (non-force) applied: fixed `fast-uri`, `fast-xml-parser`, and `postcss` (25 findings to 22), lockfile-only, no `package.json` ranges changed. Remaining 22 reviewed individually rather than forced: `react-router` (high, CSRF) has no 7.x patch - fixed only at 8.3.0+ - and the advisory states it only affects apps using react-router's unstable RSC APIs, which this app does not use; accepted as not applicable rather than forcing a major router bump. The other 21 (`brace-expansion` chain, `uuid`) are pinned entirely inside `firebase-admin@14.2.0` (the latest published version) via `google-gax` and `@google-cloud/storage` - Google's own dependency choices, not resolvable by bumping our `package.json`. `firebase-admin` is server-only (confirmed: not imported anywhere in `src/`), and `rimraf`/`glob` inside `google-gax` run only for internal SDK file cleanup, not on any user-reachable path. Do not run `npm audit fix --force`: it downgrades `firebase-admin` to 10.3.0, upgrades ESLint past the `eslint-plugin-react-hooks@5` peer range, and surfaces a new `protobufjs` chain - a net loss. Re-check when `firebase-admin` publishes past 14.2.0. |
| Split oversized components and services | In Progress | `RegisterPage` domain extraction is essentially done. Step 0 moved derived eligibility/verification gates to `src/utils/registrationEligibility.js`; steps 1-4 moved event loading, the registrant form, identity verification, and payment/seat hold into `src/hooks/`; `RegistrationPaymentPanel` moved to `src/components/RegistrationPaymentPanel.jsx` (409 lines) with its Square helpers. Step 5 moved the self-contained profile-edit flow (`needsProfileEdits`, start/cancel/save) into `src/hooks/useProfileEditing.js`. `RegisterPage.jsx` is 1151 lines (from 1949) with 8 `useState` calls (from 54). A vitest + Testing Library client runner (`npm run test:client`, `tests/client/`, in CI) covers all four stateful hooks with 65 tests. What remains in `RegisterPage()` is `handleSubmit` and its immediate state (`confirmation`, `formError`, `submitting`, `registrationFinalizing`, `closeMessage`) plus JSX - evaluated for extraction and deliberately left in place: `handleSubmit` reads from every other domain (registrant, identity, event, and payment state, ~17 distinct values) to actually submit the registration, so it is the page's legitimate orchestration point. Moving it into a hook would relocate that coupling into a parameter list rather than reduce it. EventForm, ConfigurationPanel, RegistrationPanel, UserControlPanel, and configurationService are untouched. |
| Centralize event display and registration availability logic | In Progress | Server-side capacity math lives in `api/_lib/registration-capacity.js`, shared by both `create-registration.js` call sites. Client registration eligibility and verification gates live in `src/utils/registrationEligibility.js`. The dependency-free `shared/` module this row called for now exists (`shared/registrationWindow.js`) and is imported by both `src/` and `api/`, which is how registration-window rules stay single-sourced. Remaining: `isPaidEvent` is still defined in both the client util and `create-registration.js`; the registration date *fallback* chains are still duplicated between `src/utils/eventFormat.js` and `api/_lib/public-event-feed.js`, so the feed advertises a slightly wider window than the server enforces for legacy events saved without explicit dates; and `src/utils/registrationAvailability.js` still computes capacity separately. All three are candidates to move into `shared/`. |
| Add shared validation schemas | Not Started | Keep frontend, APIs, and Firestore data contracts aligned. |
| Add route-level lazy loading | Not Started | Reduce initial JavaScript and PWA precache size. |
| Add staging Firebase, Square sandbox, and test data | Not Started | Keep registration and payment testing out of production records. |
| Add error monitoring and operational alerts | Not Started | Capture client errors, API failures, payment mismatches, and email failures. |
| Consolidate Vercel APIs or move backend functions | Not Started | Currently at the Hobby 12-function limit; prefer extending existing routes before adding new ones. |
| Update project documentation to match the current system | Completed | Spec describes current behavior; this upgrade plan is the status source of truth; README covers setup, env, API surface, and production posture. |

## Completion Log

| Date | Change |
| --- | --- |
| 2026-07-22 | Created the upgrade plan and started Phase 1 registration security work. |
| 2026-07-22 | Completed Phase 1 code: protected profile lookup, Firebase/password verification, Resend email codes, one-use registration tokens, server-only registration creation, and removal of phone verification. |
| 2026-07-22 | Verified five registration-security tests, zero changed-file lint findings, a successful production build, Firestore rules compilation, and denial of an unauthenticated direct registration write. |
| 2026-07-22 | Added the `registrationVerifications.expiresAt` TTL policy to the version-controlled Firestore index configuration. |
| 2026-07-23 | Confirmed Phase 1 production testing passed and started Phase 2. Added 5-minute Square checkout seat reservations, private `registrationReservations` rules, and TTL cleanup for expired holds. |
| 2026-07-23 | Added registration idempotency protection using private `registrationAttempts` records and Square idempotency keys to guard against double-clicks and retry-created duplicate charges. |
| 2026-07-23 | Added Square webhook endpoint with signature verification, webhook event logging, payment completion/failure reconciliation, and full-refund-only registration refund updates. |
| 2026-07-23 | Added Payment Review dashboard module for Square webhook reconciliation records that need admin attention. |
| 2026-07-24 | Started app-initiated refund controls by adding a Payment Settings toggle while preserving the current treasurer-in-Square manual refund recording workflow. |
| 2026-07-24 | Added the guarded online Square refund action to the existing admin payment endpoint. When enabled, Square must return a completed refund before the registration is marked refunded/cancelled. |
| 2026-07-24 | Updated app-initiated refunds to handle Square PENDING responses without a second click. Refund Pending now cancels the registration immediately, returns the seat, and creates a Payment Review follow-up while Square completion remains pending. |
| 2026-07-24 | Added registration cancellation/refund notification emails after admin refund actions, using the existing email toggle and coordinator reply-to contact. |
| 2026-07-24 | Refined Square refund webhook reconciliation so refund-related `payment.updated` events no longer create Needs Review rows, and completed refund webhooks clear matching pending refund review items. |
| 2026-07-24 | Fixed member-directory reads by aligning the active-member query with Firestore rule constraints. |
| 2026-07-24 | Refreshed `PROJECT_SPEC.md` and this upgrade plan to match the current app configuration and upgrade status. |
| 2026-07-24 | Completed the final Phase 2 item by adding Firestore-backed rate limits for payment, registration, verification, and refund-sensitive API flows. |
| 2026-07-24 | Doc ownership pass: corrected API count to 12, marked Phase 3 rate limiting In Progress for remaining coverage/monitoring, pointed `PROJECT_SPEC.md` upgrade priorities at this plan, and documented `apiRateLimits`. |
| 2026-07-24 | README pass: expanded `.env.example`, added Firebase deploy command, API surface, and production posture aligned with this plan. |
| 2026-07-24 | Aligned GoDaddy Programs filter with Spec/app grouping by including Retreat (and legacy hyphenated class types); corrected Spec embed category/filter wording. |
| 2026-07-24 | Added directory-safe `memberDirectoryProfiles` collection, write-through sync, backfill script, and removed Active-member peer reads of full `users` documents. |
| 2026-07-24 | Phase 3 progress: extended API rate limits, security headers, privacy/support pages, event-admin Storage rules, server-only event writes via `/api/admin-manage-event`, and folded password changes into `/api/admin-update-user-profile`. |
| 2026-07-24 | Added admin Cancel Registration for free/unpaid registrations (`No Charge`, Pending, Waived, Failed) so seats can be returned without using the refund flow; updated Spec, App Overview, and Role Capabilities docs. |
| 2026-07-24 | Cleared the source lint backlog, scoped ESLint away from generated `.vercel` output, and added a GitHub Actions CI workflow running lint, build, and tests on Node 18 and 20. |
| 2026-07-24 | Extracted registration capacity and Square reconciliation logic into `api/_lib/`, shared one capacity predicate across both `create-registration.js` call sites, and added 19 tests (suite total 9 to 28). Verified the tests catch regressions by mutation testing the capacity boundary and the payment-hold email binding. |
| 2026-07-24 | Fixed a capacity/listing divergence: events saved with capacity 0 and unlimited unchecked advertised open seats on listings and the GoDaddy feed while registration silently waitlisted everyone. Capacity is now validated to at least 1 in `EventForm` and re-checked server-side in `/api/admin-manage-event`. Existing 0-capacity events still need a one-time cleanup. |
| 2026-07-24 | Added `npm run audit:zero-capacity` to list existing events stored with capacity 0 so they can be corrected. |
| 2026-07-24 | `RegisterPage` step 0: extracted registration eligibility and identity-verification gates to `src/utils/registrationEligibility.js` with 12 tests (suite total 28 to 40). Collapsed the duplicate `isPaidEvent`/`requiresBillingAddress` expressions. Mutation-tested the registrant-field identity gate and the paid-event rule. Behavior preserved; the file itself did not shrink, since the goal was testability ahead of the hook extraction. |
| 2026-07-24 | `RegisterPage` step 1: extracted event loading and membership-settings subscription into `src/hooks/useEventRegistration.js`. `RegisterPage.jsx` is 1910 lines, down from 1949. Verified live against the `vq-event-management` Firebase project (test data): the happy path (Test Half Day Class - $13 + $1 fee = $14 total, capacity 2) and the error path (nonexistent eventId - "Event Unavailable", no console errors) both render correctly through the extracted hook. Added `.claude/launch.json` so `npm run dev` can be previewed. No client test runner exists yet, so this step relied on manual verification rather than automated coverage. |
| 2026-07-24 | `RegisterPage` step 2: extracted the registrant contact/billing form (9 states, `applyProfile`, `reset`) into `src/hooks/useRegistrantForm.js`; JSX bindings were left untouched by aliasing the hook's returns to the original variable names. `RegisterPage.jsx` is 1902 lines. Fixed a real exhaustive-deps warning this surfaced: `setFieldErrors` is no longer a same-component `useState` setter so ESLint can no longer assume it is stable; added it to `runEmailLookup`'s dependency array rather than suppressing the warning. Discovered a testing-infrastructure limit: `npm run dev` cannot exercise anything past email lookup because it does not serve the `/api/*` Vercel functions (confirmed via a 404, handled gracefully by the existing error path, no console errors) - `vercel dev` would work but requires live Firebase/Resend secrets and would send real email, so it was not run without checking first. |
| 2026-07-25 | `RegisterPage` step 5: extracted the profile-edit flow (`needsProfileEdits` plus start/cancel/save) into `src/hooks/useProfileEditing.js`, including its own `validateProfileFields`. `RegisterPage.jsx` is 1151 lines with 8 `useState` calls. Considered and declined extracting `handleSubmit` itself into a "submission" hook: it reads ~17 distinct values across every other domain to build and send the registration request, so a hook wrapping it would need the same parameter count - relocating the coupling, not reducing it. Left it as the page's orchestration point, which is where cross-domain coordination belongs. Added 12 tests for `useProfileEditing` (suite 54 to 65), including format validation for state code, postal code, and phone, and that cancel does not call `applyProfile` when there is no matched profile. Mutation-tested the cancel-restore branch. Browser-verified the email field's onChange - which now calls the hook's exposed `setNeedsProfileEdits` alongside `handleEmailChange` - fires with no console errors. |
| 2026-07-25 | Ran `npm audit fix` (non-force): fixed `fast-uri`, `fast-xml-parser`, and `postcss` (25 findings to 22), lockfile-only. Reviewed the remaining 22 individually instead of forcing: `react-router`'s CSRF advisory only affects apps using its unstable RSC APIs (this app doesn't) and has no 7.x patch, so it's accepted as not applicable; the `brace-expansion`/`uuid` chains are pinned inside `firebase-admin@14.2.0` (already the latest published version) by Google's own SDK dependencies and are not reachable from any user-facing code path. |
| 2026-07-25 | Fixed a seat-hold race in `usePaymentReservation`. When a reservation request was already running, `ensurePaymentReservation` returned `null`, so a submit landing in that window posted an empty `paymentReservationId` and the server rejected it as an expired hold. Reachable in normal use: editing any billing field drops the hold and immediately starts a new one, so typing the last field and clicking Submit hits it. Concurrent callers now await the same in-flight promise and all receive the same reservation. Still exactly one hold per registrant. Mutation-tested: restoring the `null` return fails 2 tests. |
| 2026-07-24 | Moved `RegistrationPaymentPanel` and its Square helpers (`loadSquareScript`, `validateSquarePaymentConfig`, `buildSquarePaymentRequest`, `selectSandboxTestPayment`, and the module-level script-promise cache) out of `RegisterPage.jsx` into `src/components/RegistrationPaymentPanel.jsx`. `RegisterPage.jsx` is 1194 lines, down from 1600. Component behavior and props are unchanged; browser-verified the paid registration page still renders with correct cost math and no console errors. |
| 2026-07-24 | `RegisterPage` step 4 (final domain): extracted online payment and the seat hold into `src/hooks/usePaymentReservation.js` with 24 tests. `RegisterPage.jsx` is 1600 lines with 14 `useState` calls, down from 1949 and 54. The two capacity-protecting guards moved intact: the ref-based re-entrancy lock (without it, the auto-reserve effect and an explicit submit each create a hold, consuming two seats for one registrant) and the invalidation effect that drops a hold when identity, billing, event, or payment preference changes. Both are mutation-tested. Two ordering cycles were broken deliberately: `paymentPreference` stays in `RegisterPage` because `buildRegistrationRequest` needs it, and the lookup-time reservation reset reaches the hook through a ref since identity must be constructed first. Also moved `getEventPaymentTotal` into `registrationEligibility.js` so the page and hook share one definition. The new tests caught a real regression introduced during the extraction: the hook kept its own `paymentPreference` state while the page passed one in, so choosing cash/check later would still have taken a Square seat hold. |
| 2026-07-25 | Found and fixed the real cause of profile saves failing: the `users` update rule could not be evaluated. Firestore charges every expression a rule evaluates against a hard ceiling of 1000 per request, and a write that will be refused has to evaluate all three branches before it can answer "no" - so a refusal costs the sum of them. The rule called `validUser()`, a thirty-field sweep, inside each branch; rules do not memoise, so one update evaluated it up to three times and blew the ceiling. Firestore reports that as a plain `permission-denied`, which is why it looked like an ordinary rules refusal, why deploying changed nothing, why every clause passed when checked by hand against the live document, and why it failed identically for a General User and an Admin. The emulator surfaced it in one run: "Unable to evaluate the expression as the maximum of 1000 expressions to evaluate has been reached." Three changes: owner self-edits now use `validSelfProfileEdit()`, a small `diff().affectedKeys()` validator that also pins the change to seven fields, making role, status, permissions and every membership field immutable on that path by construction rather than by twelve separate comparisons; `membershipFieldsUnchanged()` is rewritten from twelve three-clause comparisons to one `affectedKeys().hasAny()`; and the admin branches share a single `validUserAdminUpdate()` that keeps the key whitelist and the role/status/permissions integrity but no longer re-type-checks fields the write is not touching (`validUser()` still guards `create`). The deliberate trade is that an admin could store a badly typed non-privilege field such as a numeric phone. `profileTagsUnchanged()` became unused and was removed. Added `tests/rules/users-self-update.test.js` (9 tests, `npm run test:rules`, wired into CI with a JDK step) - the project's first Firestore rules tests, covering owner save, refusal of role/permission/membership escalation, cross-user writes, and both admin paths. The negative tests assert the refusal was on the merits and not another expression-limit failure, since `assertFails()` passes for both and would otherwise stay green while the rule was broken - exactly how this hid. Requires `firebase deploy --only firestore:rules`. |
| 2026-07-25 | ~~**Open issue - deployed Firestore rules are behind `firestore.rules`.**~~ Superseded by the entry above: the deployed rules were not stale, the rule itself could not be evaluated. Recorded because the wrong diagnosis cost a deploy cycle - every clause of the owner branch was verified by hand against the live document and all of them passed, which ruled out the content of the rule but not its evaluation cost. The emulator was what distinguished the two, and reading the file could not have. A General User cannot save their profile in production: every write to their own `users/{uid}` document is rejected with `permission-denied`, including a single-field update sending the value it already held. The repo's rules contain an owner self-update branch (added in `7b548f2`) that permits exactly this, and the account satisfies every clause in it - checked field by field against the live profile: all keys whitelisted, all timestamps real timestamps, all five permission booleans present and false, role General User, status and membershipStatus Active, `profileTags` empty. Isolated the failing write by running the batch's two halves separately from the page: the `memberDirectoryProfiles` write succeeds, the `users` write is denied. Nothing in the client can fix this - run `firebase deploy --only firestore:rules` and re-test. This also means profile saving has been broken for ordinary members for some time; it went unnoticed because the error was being erased before it could render (see the same-day entry below). |
| 2026-07-25 | Fixed the profile page swallowing save failures. `handleSubmit` calls Firebase Auth's `updateProfile` before writing to Firestore; that success re-emits `currentUser` with a fresh identity, which re-created the `resetProfileForm` callback and re-ran its effect. Because that function also cleared `formError` and `successMessage` and reset every field, a failed save erased its own error a moment after setting it and reverted the user's input - so the page reported nothing at all, which is what made a broken save look like a dead button. The sync is now field-values-only, runs only while not editing, and message clearing belongs to whoever ends the edit. Also mapped `permission-denied` and `unavailable` to plain language rather than showing "Missing or insufficient permissions.", which is not something a member can act on (a start on the Phase 6 raw-Firebase-errors row). Added 5 tests (client 100 to 105) including the first successful-save test this page has had - its absence is precisely why the swallow survived the previous round: three mutations initially survived because every existing test exercised only the refusal path. Mutation-tested: restoring the message clearing, letting the sync run while editing, reverting to the raw Firebase message, and dropping the success path's exit from edit mode each fail one test. |
| 2026-07-25 | Gave `/profile` a read-only view, which is what the "cancel profile edit doesn't work" report was actually about. The page opened straight into an always-editable form, so Cancel had nothing to return to: with no edits made it reset the fields to the values they already held and changed nothing on screen, which reads as a dead button. Confirmed by reproducing it as a general user - clicking Cancel left the values, the URL, the form, and the messages all identical. The card now shows Name, Email, Phone, and Billing Address in a `detail-panel` list with an Edit Profile button; editing reveals the existing form, and Cancel reverts and returns to the view. Save returns to the view too, while a refused save stays in the form so input is not discarded. This follows the admin `UserControlPanel`, which already worked view-then-edit; `/profile` had never had a view mode, so this is new behaviour rather than a restored regression. Added `formatBillingSummary` for the one-line address. 5 tests for it (server 77 to 81) and 8 for the page (client 91 to 100), the first component tests for `ProfilePage`. Mutation-tested five: opening in edit mode again fails 7 tests, Cancel not exiting fails 3, Cancel not reverting fails 2, a failed save exiting edit mode fails 1, and dropping the address comma fails 1. One test submits the form directly rather than clicking Save, because clearing a `required` field means native constraint validation swallows the click and the page's own trim() check is never reached. |
| 2026-07-25 | Fixed two defects behind a "cancel profile edit doesn't work" report. First, `/profile` crashed to the error boundary whenever `userProfile` was null: `getProfileFirstName`/`getProfileLastName` declared `profile = {}`, and a default parameter only fills in for `undefined`, so the explicit null that `AuthContext` starts with skipped it and threw on property access. `ProfilePage`'s mount effect calls those helpers, and effects run regardless of the early returns below them, so the throw beat both the `loading` guard and the `!currentUser` redirect - a signed-out visit crashed instead of going to `/login`, and a signed-in direct load or refresh crashed if the Firestore profile had not arrived yet. Reproduced (React error boundary, "Cannot read properties of null (reading 'firstName')") and confirmed fixed by hard-reloading `/profile`. `toTitleCase` now coerces rather than assuming a string, which is the single load-bearing guard for the whole path. Second, `handleCancelProfileEdit` restored the values but left `fieldErrors` and `formError` set; since the form error renders far above the edit block, cancelling after a failed save closed the editor while "Please fix the highlighted profile fields before saving." stayed on screen with fields still marked invalid. Added 12 tests for `profileFormat` (server suite 66 to 77, the first coverage this module has had) and 1 for the cancel path (client 90 to 91). Mutation-tested: restoring the default parameter, dropping the `toTitleCase` coercion, and dropping the cancel cleanup each fail. A fourth guard added during the fix - a redundant `|| ''` in `splitDisplayName` - survived its mutation, meaning no test could justify it, so it was removed rather than left in. |
| 2026-07-25 | Moved the mobile site nav into a slide-over sheet. Below 760px the sticky header stacked every destination as its own bordered pill and then added the `auth-banner` strip underneath, which measured 364px of a 375x812 viewport - 45% of the screen occupied before any content, and sticky, so it followed you down the page. The header is now a single row of brand plus menu button (98px, 12%), and the destinations live in `src/components/MobileNavSheet.jsx`. Layout follows a settings-screen reference the user supplied: one rounded card per group with inset hairline dividers, rather than six separately-bordered boxes with gaps - the gaps and doubled borders were a real part of the height, not just the row count. Icons were considered and declined: the app has no SVGs or icon library anywhere, so a per-row icon set would have introduced a visual vocabulary nothing else uses; the menu bars, close X, and chevrons are drawn in CSS. Desktop is untouched - the nav row, its wrapping, and the auth banner all render as before, and the menu button is `display: none` above the breakpoint. Both the desktop row and the sheet render from one `navDestinations` list in `App.jsx` so they cannot drift as permissions change. Treated as a real dialog since it covers the page: `role="dialog"`, `aria-modal`, focus moved to the close button on open, Tab trapped and wrapping at both ends, Escape to close, body scroll locked, backdrop tap to dismiss, and focus restored to the menu button on every close path - which also starts the Phase 6 focus-management row. Rows close the sheet on selection, and sign-out closes before signing out so focus is restored before the tree changes. The sheet is styled for any width, so opening it on a phone and then widening the window leaves it usable (caps at 420px, right-aligned) rather than broken. 16 tests (client suite 74 to 90); mutation-tested six - dropping focus-on-open, Escape, the scroll lock, the tab trap, the close-before-sign-out ordering, and close-on-select each fail exactly one test. Verified live at 375px and 1280px with no console errors. |
| 2026-07-25 | Enforced the registration open/close dates, which were displayed to members but honoured by nothing. `EventForm` writes `registrationOpen: registrationMode === 'now'` once at save time and never revisits it, and that boolean was the only thing every gate read - so a "Now" event never closed after its close date, and an "In The Future" event never opened at all, while the form promised "Members can only register between these two times." Availability is now derived from `registrationOpenAt`/`registrationCloseAt` by a new dependency-free `shared/registrationWindow.js`, imported by both `src/` and `api/`; the stored boolean is no longer read anywhere. Timezone was the subtle part: the admin form writes naive `datetime-local` strings, `Date.parse` resolves those against the *runtime* zone, and Vercel runs UTC - so a runtime-local parse would have closed registration four hours early in production only. The module pins `America/New_York` via `Intl`, re-measuring the offset so times inside a DST changeover resolve correctly. Enforced in `/api/create-registration` as well as the client, so a closed event cannot be registered for by replaying a request. Also updated the public feed (derived from the same values it publishes, so it cannot advertise a close date it ignores), all four listing views (`EventsPage`, `EventDetailsPage`, `EventListingPrintPage`, admin `EventList`), and added `EventForm` validation rejecting a close date at or before the open date - previously an inverted range saved cleanly and rendered "Opens 07/19/2026 - Closes 07/13/2026" to members. Surfaced that message through the existing `<small>` field-error pattern, which the two registration date inputs were missing, so the admin sees why rather than just a red box around a field that already has a value. 24 tests for the shared module (suite 40 to 66); mutation-tested four - a runtime-local parse, a single-pass offset, reverting the gate to the stored boolean, and an exclusive open boundary each fail. The single-pass mutation initially survived, because the DST tests sat a day either side of the transition where one pass is already correct; added two cases inside the changeover itself to close that. Verified live: three of the four test events flipped from "REGISTRATION OPEN" with a live Register link to "REGISTRATION CLOSED" with none, the register page reports "Registration for this event closed 07/21/2026", and re-saving the inverted event is refused with no write (stored dates confirmed unchanged afterwards). The `/api/*` paths could not be exercised locally - `npm run dev` does not serve the Vercel functions - so the server gate rests on unit tests and review rather than a live request. |
| 2026-07-25 | Split the admin dashboard `Manage/Edit` row for mobile - the follow-up deferred by the senior-user UI review commit. Below 760px the row collapses behind one toggle that names the active module ("Manage/Edit: Registrations"); it expands to full-width stacked buttons and closes itself on selection. Collapsed height is 48px against 464px of always-open buttons before. Desktop is byte-identical: the new wrapper uses `display: contents` above the breakpoint so the buttons stay direct flex children of `.admin-module-nav` and wrap inline with the "Manage/Edit:" label. Measured both layouts in the browser against a reconstruction of the pre-change DOM - nav height 107px, label at 3px, button tops `[0,0,0,0,0,0,60]` - identical. A first attempt using a real flex wrapper regressed desktop (label pushed onto its own line, 102px) and was caught by that comparison, not by lint or tests. The eight permission-gated buttons now render from one `manageModules` array, which is also what supplies the toggle's label. Added 9 component tests (suite 65 to 74), the first in this project to render a page rather than a hook; they needed an explicit `afterEach(cleanup)` because `vitest.config.js` does not set `globals: true`, so Testing Library never registers its automatic cleanup and renders leak between tests. Mutation-tested three: dropping the close-on-select, blanking the "Choose a section" fallback, and leaking Setup / System Config to non-super-users each fail the intended test. |
| 2026-07-24 | `RegisterPage` step 3: extracted identity verification (email lookup, password sign-in, emailed-code fallback - 18 states and 4 async handlers) into `src/hooks/useIdentityVerification.js` with 22 tests. `RegisterPage.jsx` is 1747 lines and its `useState` count is down from 54 to 22. `runEmailLookup` also resets submission/payment-reservation state that belongs to `RegisterPage`, so rather than absorb unrelated state the hook takes `onBeforeLookup` and `setFormError` from the caller. Tests cover the security-relevant paths: the server's `verified` verdict overrides the caller's optimistic `alreadyVerified` hint, changing the email clears prior verification proof, and a failed code attempt clears the registration token. Mutation-tested both of those - each fails a test when broken. Browser-verified the no-API paths (event renders, invalid-email validation shows the error and issues no network request). |
| 2026-07-24 | Added a client test runner: vitest + `@testing-library/react` + `@testing-library/jest-dom`, config at `vitest.config.js`, tests under `tests/client/`, run via `npm run test:client` and wired into `.github/workflows/ci.yml` as a separate CI step alongside the existing `node --test` suite. Wrote 8 tests for `useRegistrantForm` (default state, `applyProfile` including the display-name-splitting fallback, `reset`, that `reset` intentionally leaves `fieldErrors` untouched, and that `applyProfile`/`reset` stay referentially stable across renders). Mutation-tested two of them: dropping the billing-country fallback and making `reset` also clear `fieldErrors` both fail a test. `npm audit` unchanged at 25 findings after adding the new dev dependencies. This closes the verification gap from the previous entry and unblocks the identity-verification and payment/seat-hold hook extractions. |
| 2026-07-26 | Added admin-initiated registration. Started as "add helper text to a checkbox" - tracing what two Membership Check settings actually did found neither was wired up at all. Removed the dead `allowAdminSkipMembershipCheck` toggle entirely (stored, rendered, never read). Wired the other, `requireMembershipCheck`, into a real global override via new `api/_lib/membership-settings.js`, threaded into both `create-registration.js`'s eligibility gate and `registration-lookup.js`'s status check - fail-closed (missing or malformed settings behave as if it were on, matching current always-on production behavior, so wiring the read in changed nothing until someone deliberately flips it off). The actual ask underneath the checkbox was letting one admin at a time register a member on their behalf, gated by a new individually-assignable `registerOthers` permission rather than a global bypass. That permission touches seven hardcoded enumeration sites, all changed together in one commit rather than staged: `userRoles.js`, three `firestore.rules` functions (including `hasNoAdminPermissions()` - the actual privilege-escalation guard, since without it demoting an Admin silently lets them keep `registerOthers: true`), both `admin-update-user-profile.js` and `admin-create-user.js` copies of `getPermissionsForRole()`, and `configurationService.js`'s `normalizeUserPermissions()` - confirmed by reading the call chain that this last one backs live archive/reactivate-membership writes (a full-document replace), not just CSV import as first assumed, so skipping it would have silently wiped the permission on any admin's next archive/reactivate. New server authorization path (`authorizeAdminRegistrationRequest`/`canRegisterOthers`) is structurally separate from the existing self-registration path (gated on a literal `'admin'` kind string), re-reads the caller's own profile fresh from Firestore by their verified token's uid, and re-checks the paid/no-cash-check guard server-side as defense in depth. Registration documents gained four `registeredByAdmin*` audit fields, added to `validRegistration()`'s whitelist in the same change - required because the existing self-cancel path re-validates the full resulting document, not a diff, so skipping it would have broken a member's next self-cancel of an admin-registered signup with an unrelated-looking "permission denied." New `AdminRegisterMemberPanel.jsx` lives in the Registrations card (`RegistrationPanel.jsx`), per-event since the event is already fixed by context: member search/select adapted from `UserControlPanel`'s filter, `useRegistrantForm`'s `applyProfile` for prefill, cash/check-only payment messaging in three states (free, paid-supported, paid-unsupported-disabled-with-explanation), and a refusal (not consent-by-proxy) for reactivating an inactive member's profile. Verified live signed in as a real Admin account predating this permission: button correctly absent without the permission, present and working with it (search, select, prefill, payment messaging, close/cancel, expected 404 against the local dev server which doesn't serve `/api/*`), no console errors. 33 new/extended tests across server, rules, and client suites (112/112 `node --test`, 14/14 Firestore rules, 140/140 vitest), lint and build clean. One test file (`tests/client/adminRegisterMemberPanel.test.jsx`) initially stalled with `userEvent.type` only ever leaving the search input holding the first typed character; turned out not to be a test-harness quirk but a real bug in the shared `ModalDialog` primitive (`src/components/ModalDialog.jsx`): its focus-trap effect depended on the `onClose` prop, which every consumer passes as a fresh inline closure, so any state update while a modal is open (here, every keystroke) tore down and re-ran the effect and re-stole focus to the dialog's first focusable element - the header's Close button, which sits before the search input in DOM order. Fixed by holding the latest `onClose` in a ref and dropping it from the effect's dependency array, so focus is only established once per open/close transition rather than on every re-render; this affects every `ModalDialog`/`ConfirmDialog` consumer in the app, not just this new panel. Mutation-tested the new guards per this repo's convention: `hasNoAdminPermissions()`'s new clause, the admin-kind structural bypass, and the `validRegistration()` whitelist addition each fail a dedicated test when reverted. Requires `firebase deploy --only firestore:rules`, and the manual verification checklist (grant `registerOthers` to a real account via User Controls, then exercise the full flow) still needs to be run for real once that's deployed. |
| 2026-07-25 | Closed the last two gaps in the Phase 6 focus-management row. The supply-list viewer opens as a real popup window rather than an in-page overlay, so it needed dialog-equivalent behavior added directly rather than `ModalDialog`'s Tab trap: focus now moves to the document heading once it loads, Escape closes it, and a new `src/utils/popupWindow.js` helper (used by both `EventDetailsPage` and `EventsPage`) polls the popup for `closed` and restores focus to the button that opened it. Separately, audited `styles.css` for focus-visible coverage and found only one deliberate pattern in use (a `#cfe3de` outline on form inputs and the registration event card) with no baseline for plain buttons/links, which fell back to inconsistent browser defaults; added one global `:focus-visible` rule reusing that same color so every interactive element gets a consistent keyboard-only focus ring. 4 tests (client suite 115 to 119); mutation-tested both new `SupplyListViewerPage` effects. |
| 2026-07-26 | Four follow-up refinements to admin-initiated registration, all from direct user feedback after the feature landed. First, a case-by-case cash/check override: an admin can now register a member for cash/check payment even on an event that doesn't otherwise offer it (for a member who cannot pay online regardless of the event's own settings), via a new checkbox in `AdminRegisterMemberPanel.jsx` and a matching `allowCashCheckOverride` flag honored only for `authorization.kind === 'admin'` requests server-side. Extracted the shared `isCashCheckPaymentAllowed()` predicate into `api/_lib/registration-capacity.js` so both `create-registration.js` call sites (the main transaction and the self-service Square reservation path) use one definition instead of two near-identical inline expressions - the self-service path never has an admin-kind authorization, so the override is structurally inert there. Second, duplicate-registration detection moved from a submit-time server 409 to the moment the admin selects a member: `RegistrationPanel.jsx` now passes the event's existing registrations down, and the panel checks the same status list and userId-or-email match `create-registration.js` uses, surfacing the warning immediately and disabling submission rather than waiting for a rejected request. Third, when the event has zero seats remaining (computed client-side via the existing `getRegistrationAvailability()` util, reused rather than re-derived), the submit action relabels to "Add Member To Waitlist" with explanatory copy, so an admin can no longer click something labeled "Register Member" for a full event and be surprised by a silent waitlist outcome - the underlying call is unchanged since the server already auto-waitlists on capacity, this only fixes the framing. Fourth, moved the "Register A Member" button from the card header to the footer beside the existing "Return" action, per user direction to keep actions co-located consistently rather than defaulting to "sibling of the title" (saved as a standing memory for future UI placement decisions). Also fixed an unrelated but user-flagged visual bug while working in this area: `.button-link` had no `align-items`, so `.compact-action`'s reduced padding left a visible gap below the label inside the base rule's `min-height: 44px` - confirmed by rendering the exact class combination in isolation before and after: the button was visibly taller than its text needed. Added `align-items: center` to `.button-link`, fixing every compact button site-wide, not just this one. 8 new client tests (suite 140 to 148) and 4 new server tests for `isCashCheckPaymentAllowed` (suite 112 to 116); full suite green (116/116 server, 14/14 rules, 148/148 client), lint and build clean. Live browser verification was not performed this round - no authenticated admin session was available in this environment - so the manual verification checklist from the original entry still applies before this reaches production. |
| 2026-07-26 | Three more fixes to admin-initiated registration, reported after the first live use of the cash/check override. First, the reported "payment status pending, doesn't show up for review" was two things at once, not one bug: the registration lands with status `Registered` (a seat is held) but paymentStatus `Pending` (money not yet collected) - that pairing is how every cash/check registration in this app has always worked, self-service included, and there is no separate "Payment Review" queue for it (that module, `PaymentReconciliationPanel.jsx`, is Square-webhook-only; cash/check status lives in the Registrations table's Payment column and its Payment Status filter). The user's own follow-up named the real fix: since the member typically hands the admin cash/check at the moment they ask to be registered, added a "Payment was already received" checkbox plus a Cash/Check method choice to `AdminRegisterMemberPanel.jsx`. New pure `resolveAdminCollectedPayment()` in `api/_lib/registration-capacity.js` (admin-kind only, never honored for a self-registrant, and only applied when the registration actually lands as `Registered` - not `Waitlisted`, since there's no confirmed seat to collect payment for yet) sets `paymentStatus: 'Paid'`, `amountPaid: amountDue`, and `paymentMethod` immediately at creation, matching the exact Cash/Check-requires-a-method convention `admin-update-registration-payment.js` already established for manual payment edits - so an admin who collected payment no longer has to submit, then immediately reopen the registration to mark it paid. Second, `AdminRegisterMemberPanel.jsx` let an admin edit the selected member's name/phone/billing address and silently wrote those edits back to the member's own profile via `profileUpdates` (confirmed by reading `create-registration.js`'s `transaction.update(db.collection('users')...)` call, shared with the self-service "edit my own profile during registration" flow) - reported as "it shouldn't allow editing," which was correct: this form registers a member for an event, it does not edit their profile. Name and phone are now read-only, pulled straight from the profile; `profileUpdates` is no longer sent at all; and an incomplete profile (missing name, or fewer than 10 phone digits) now blocks submission with a message pointing at User Controls, rather than letting the admin patch it inline. This also deleted the entire billing-address section from this form - it turned out to serve no purpose here at all, since billing address is never stored on the registration document, only ever written to the profile via the `profileUpdates` path this change removes. Third, the full-event "Add Member To Waitlist" button relabel from the prior entry was reported as "too subtle - I didn't notice it" - replaced with a blocking `ConfirmDialog` ("Event Full - Add To Waitlist Instead?") that an admin must explicitly confirm before the waitlist submission proceeds, following the same nested-modal pattern this file already uses for its refund/cancel confirmations. 16 new/rewritten client tests and 5 new server tests for `resolveAdminCollectedPayment`; full suite green (121/121 server, 14/14 rules, 154/154 client), lint and build clean. Live browser verification still not performed in this environment. |
| 2026-07-26 | Gave cash/check payments awaiting collection an actual place to be managed, and a second, easier-to-find entry point into admin registration - both directly requested after using the feature for the first time. `PaymentReconciliationPanel.jsx` ("Payment Review") gained a "Cash/Check Awaiting Collection" section above its existing Square-webhook table: subscribes to all registrations (same pattern `RegistrationPanel.jsx` already uses) and filters to `status === 'Registered' && paymentStatus === 'Pending' && paymentPreference === 'cash-check-later'` - deliberately excluding Waitlisted (no seat held, nothing to collect for yet) and anything already Paid. Each row offers a Cash/Check choice and a "Mark Paid" action that calls the existing `updateRegistrationPayment`/`admin-update-registration-payment.js` endpoint - the same manual-payment path `RegistrationPanel.jsx`'s Edit Registration modal already uses, so this is a second, faster entry point onto established code, not a new payment-writing path. Gave the pre-existing Square section its own heading ("Square Webhook Reconciliation") now that the card holds two distinct lists. Second, added a "Register A Member" button directly to each event's summary card in the Registrations list (next to "View/Edit Registrations"), not just in the drilled-in event view's footer from the prior entries - it drills into the event and opens the registration panel in one click, so an admin no longer has to open "View/Edit Registrations" first just to find the button. 6 new client tests for the Cash/Check Awaiting Collection filtering and Mark Paid flow; full suite green (121/121 server, 14/14 rules, 160/160 client), lint and build clean. Live browser verification still not performed in this environment. |
| 2026-07-26 | Three more admin-registration fixes from the first real attempt to register an inactive member. First, explained and fixed a confusing refusal: "Please confirm whether you want to reactivate the matched profile." is `validateRegistrationEligibility()`'s existing gate on the member's account status (`profile.status !== 'Active'`) - unrelated to event registration and worded for a self-registrant choosing to reactivate their own profile, which reads as a non sequitur to an admin (who never sees a reactivation option in this form at all, and was not asking about a duplicate registration, just confused by wording that didn't match anything in the UI). `AdminRegisterMemberPanel.jsx`'s `getProfileIssue()` now checks the selected member's account status up front - mirroring `create-registration.js`'s `getProfileStatus()` exactly, including the archived-fields fallback - and blocks with "This member's account is Inactive/Archived, not Active. Reactivate them via User Controls..." before the server is ever asked, so that server message should no longer be seen from this flow. Second, added a registration-status pill ("Registration Open" / the specific closed-or-not-yet-open reason) to each event's summary card in the Registrations list, reusing the existing `getRegistrationUnavailableReason()` util the public register page already relies on - so an admin can see whether registration is actually open before trying to register someone for a closed event, without having to drill in first. Third, changed the registrant table's default sort from alphabetical-by-name to registration date descending (newest first) - the one registration-list sort config in the app - so newly-registered members surface at the top by default instead of requiring a manual re-sort. 3 new client tests for the inactive/archived-account gate; full suite green (121/121 server, 14/14 rules, 162/162 client), lint and build clean. Live browser verification still not performed in this environment. |
| 2026-07-26 | Fixed a real false positive in the account-status gate from the entry above, reported immediately after trying to register a genuinely active member and being refused. `getProfileStatus()` (`create-registration.js`) had no Super User exception, even though `UserControlPanel.jsx` and `configurationService.js` both already treat Super User accounts as always Active when displaying or saving a profile (`role === 'Super User' ? 'Active' : ...`) precisely because Super User accounts are not managed through the ordinary status field at all - the very first admin/owner account in particular is often bootstrapped outside the normal Add User flow and may never have `status` set to the literal string 'Active'. Without the exception, such an account reads as not Active and gets refused with a reactivation prompt that makes no sense for an account that was never deactivated. Added the same Super User exception to `getProfileStatus()` (now exported for testing) and to `AdminRegisterMemberPanel.jsx`'s mirrored client-side check, keeping both consistent. Left the "no stored status at all" case for ordinary (non-Super-User) profiles refusing as `Unknown`, unchanged - broadening that further wasn't needed to fix the reported case and is a more consequential security-adjacent change to make without stronger evidence it's wanted. Separately, restructured the Registrations list card layout per direct feedback: the info pills (registration status, capacity, registered/pending/waitlisted/cancelled counts) now render as one unstacked row across the top of the card instead of wrapping inside a narrow right-hand column, the duplicate Capacity value that appeared in both the pill row and the Date/Time/Location/Cost details was removed from the details side (`EventInfoPills` gained a `showCapacity` option, defaulting true so the drilled-in event view is unaffected), and "View/Edit Registrations" plus "Register A Member" now sit side by side instead of stacked. This collapsed the card from a two-column desktop layout down to one already-mobile-friendly stacked-sections shape, so the mobile-only overrides for the old two-column arrangement were removed as dead code rather than left stacked on top of a rule that no longer needs them. 6 new server tests for `getProfileStatus()` and 1 new client test for the Super User case; full suite green (126/126 server, 14/14 rules, 163/163 client), lint and build clean. Verified the new card layout by rendering the exact markup against the real stylesheet through the Vite dev server (not a full authenticated flow): pills in one row, no duplicated capacity, both buttons side by side. |
| 2026-07-26 | Added Account Status and Membership Status to `AdminRegisterMemberPanel.jsx`'s read-only member detail grid (alongside the existing Phone), on request - useful context for the admin at exactly the moment they've picked a member, not just when something is already wrong. Reused the account-status gate's own `getDisplayAccountStatus()` and added a matching `getDisplayMembershipStatus()`, mirroring `UserControlPanel.jsx`'s existing convention that a Super User's membership status displays as `N/A` rather than their (usually absent or irrelevant) `membershipStatus` field, since Super Users aren't tracked as Guild members. 3 new client tests (both fields display, a non-Active membership status shows distinctly from account status, and the Super User N/A case); full suite green (126/126 server, 14/14 rules, 166/166 client), lint and build clean. |
| 2026-07-26 | Fixed a real gap reported against an event with two registrations genuinely awaiting cash/check collection: both the Registrations list's stat pills and Payment Review's "Cash/Check Awaiting Collection" section showed 0. Root cause: `isCashCheckAwaitingCollection()` additionally required `paymentPreference === 'cash-check-later'`, but a confirmed (Registered), still-unpaid (paymentStatus Pending) registration for a paid event can only exist via the cash/check path in the current code - so that third condition was redundant on paper, but silently excluded any registration missing the field (older records predating `paymentPreference`, or one an admin reset to Pending via the Edit Registration payment form). Dropped that condition; `status === 'Registered' && paymentStatus === 'Pending'` is now the sole, sufficient test. Extracted the same predicate into a shared `reduceRegistrationCounts()` in `RegistrationPanel.jsx` and added a matching "X Cash/Check Due" stat pill to the Registrations list card, so the two views (event card and Payment Review) now agree instead of the event card's differently-scoped "Pending Payment" pill (registration status, i.e. an incomplete Square checkout) reading 0 for a situation the admin reasonably called "pending payment." Also simplified the registration-status pill added two entries ago: replaced the generic "Registration is not currently open for this event." sentence (and the full "Registration for this event closed {date}." sentence) with a short, consistent "Registration Closed" label, reading `getRegistrationWindowState()`'s `state` directly instead of the full-sentence `getRegistrationUnavailableReason()` util - a pill is glanceable, not a sentence, and the close date is still visible in the drilled-in event's own details. Separately, removed the "Show/Hide Registration Details" toggle from the public Programs & Activities list (`EventsPage.jsx`): the Capacity/Registered/Pending Payment/Waitlisted breakdown had been deliberately collapsed behind a click since Phase 6 (documented reasoning: "members mainly need one number - is there a seat?") but now always renders alongside the primary Open Seats pill, per direct request. 1 rewritten client test replacing an assertion that a blank `paymentPreference` should be *excluded* (the exact behavior just identified as the bug) with two assertions that it is *included*, both blank and entirely absent; full suite green (126/126 server, 14/14 rules, 166/166 client), lint and build clean. |
| 2026-07-26 | The previous entry's fix turned out incomplete: verified live against the real `vq-event-management` Firebase project (signed in as `kurtszeluga@hotmail.com`, real Admin) via Chrome that Witch Hat Wizardry's 2 cash/check registrations were correctly caught by the new "Cash/Check Due" pill and the Payment Review section - but the user's actual mental model was that these should read as "Pending Payment," and the dashboard's "Payment Review" Needs Attention badge (driven only by Square webhook needsReview events) still showed 0 for the same 2 records. Consolidated three independent copies of this predicate logic (`PaymentReconciliationPanel.jsx`'s local function, `RegistrationPanel.jsx`'s inline check, and a new one that would have been needed in `AdminDashboardPage.jsx`) into two shared, exported, tested functions in `src/utils/registrationEligibility.js`: `isCashCheckAwaitingCollection()` (Registered + paymentStatus Pending - the actionable set Payment Review's Mark Paid action applies to) and a broader `isPaymentPending()` (also true when status is the literal 'Pending Payment' - an incomplete online card checkout, which reads as "payment pending" too even though it is not something the cash/check Mark Paid action applies to). The Registrations list's "Pending Payment" stat pill now uses `isPaymentPending()` directly - the separate "Cash/Check Due" pill from the previous entry was removed as redundant since its count is now folded into "Pending Payment" itself, undoing that part of the prior entry once the right fix was clear. `AdminDashboardPage.jsx` gained a second `subscribeToRegistrations` listener counting `isCashCheckAwaitingCollection()` matches, summed with the existing Square needsReview count into one `paymentReviewCount` - the badge now equals what an admin would see across both sections of the Payment Review page combined. Also added a confirmation dialog (matching the existing `ConfirmDialog` nested-modal pattern used elsewhere) before Payment Review's "Mark Paid" action actually writes anything, on request - marking a payment received is a real accounting action, not something a single stray click should complete. Re-verified live after the fix: the dashboard badge and the event card's "Pending Payment" pill both now read 2 for Witch Hat Wizardry, matching the 2 rows in Payment Review's Cash/Check Awaiting Collection section. 6 new server tests for the two shared predicates, 1 rewired test mock (`adminModuleNav.test.jsx` needed `subscribeToRegistrations` added to its `registrationService.js` mock once `AdminDashboardPage.jsx` started calling it), and the Mark Paid tests split into three (opens confirmation / cancel does not submit / confirm submits) since the action is no longer a single click; full suite green (128/128 server, 14/14 rules, 168/168 client), lint and build clean. |
| 2026-07-26 | Made archiving an event carry its registrations with it, on request: an archived event's registrations were staying listed in the admin Registrations card indefinitely, with no relationship to the event's own archived state. Archiving is now blocked (409, `api/admin-manage-event.js`) whenever any of the event's registrations still has a payment awaiting collection - queried fresh from `registrations` by `eventId` and judged by a new pure `getArchiveBlockError()` in `api/_lib/registration-capacity.js`, which reuses the existing `isPaymentPending()` predicate. Deliberately does not count Waitlisted registrants: they hold no seat and owe nothing, so they never hold up an archive, and they archive along with everything else once it succeeds. Moved `isCashCheckAwaitingCollection()`/`isPaymentPending()` out of `src/utils/registrationEligibility.js` into a new dependency-free `shared/registrationPayment.js` (the established pattern from `shared/registrationWindow.js`) so the server check and every existing client caller share one definition; `registrationEligibility.js` re-exports both, so no existing import or test needed to change. While wiring the block up, found `AdminDashboardPage.jsx`'s archive-confirm handler had no `catch` at all - a refusal would have thrown into an unhandled promise rejection with nothing shown to the admin, and the "busy" flag would have quietly cleared as if nothing happened. Added an `error` prop to the shared `ConfirmDialog` (used by several other admin flows too) and a `catch` that surfaces the server's message and keeps the dialog open so the admin can go resolve the payments first. Separately, `RegistrationPanel.jsx` gained an Active/Archived event-status filter, mirroring `EventList.jsx`'s own filter and defaulting to Active - archived events (and, via a matching check on the panel's orphan-registration fallback path, their registrations) drop out of the default view together, but nothing is deleted and the full event history stays one click away behind the filter. 6 new server tests for `getArchiveBlockError` (pending blocks, waitlisted doesn't, paid/refunded/no-charge don't, pluralization), 2 new client tests for the dashboard error surfacing and recovery, and 3 new client tests for the Registrations-card filter (hidden by default, shown when selected, restored by Reset Filters); full suite green (134/134 server, 14/14 rules, 173/173 client), lint and build clean. Live browser verification not performed in this environment. |
| 2026-07-26 | Added the Archive module and admin-only Total Paid figures requested right after the archive-cascade entry above. New "Archive" tab (`src/components/admin/ArchivePanel.jsx`) lists every archived event across all types, drills into one for a read-only registrant table, and prints a report via the same no-route `window.open`/`document.write` pattern `EventsPage.jsx`'s public listing print already uses - built from data already on hand, so it needed no new route, re-fetch, or auth re-check. Un-archive reuses `EventList.jsx`'s existing `handleDelete`/`ConfirmDialog` archive-reactivate flow unchanged; because archiving never deletes registration data, reactivating needed no new plumbing to bring registrations back. Confirmed with the user up front: the tab shows for anyone with `manageEvents` even without `viewRegistrations` (Un-archive still works; the registrant table, totals, and Print Report are simply hidden), and the "Total Paid" figure is always computed live rather than frozen at archive time. New pure `getTotalPaidAmount()` (`src/utils/registrationFinancials.js`) sums `amountPaid` only where `paymentStatus === 'Paid'` - already excluding Refunded/Refund Pending, since `amountPaid` is kept as history after a refund but `paymentStatus` moves off `'Paid'`. That same figure now also shows as a "Total Paid" stat pill on paid events in the Manage Events list (`EventList.jsx`) and on the Registrations card's own event summary cards (`RegistrationPanel.jsx`) - admin-only in both cases, never on any public listing; `AdminDashboardPage.jsx` only passes the figure through when the signed-in admin has `viewRegistrations`. Consolidated the dashboard's registrations subscription along the way: the Payment Review badge previously ran its own `subscribeToRegistrations` counting only cash/check-awaiting registrations - it's now one subscription that also derives `registrationsByEventId` and `totalPaidByEventId`, so the page reads the `registrations` collection once instead of once per consumer. 6 new server tests for `getTotalPaidAmount` (Paid-only summing, refund netting, waived/no-charge exclusion, empty/null safety), 6 new client tests for `ArchivePanel` (archived-only listing, empty state, drill-in with a waitlisted registrant correctly excluded from the total, permission degradation, un-archive, print), 3 for the `EventList` pill, 1 for the `RegistrationPanel` pill, and a 1-line fix to the existing admin-module-nav order test; full suite green (140/140 server, 14/14 rules, 183/183 client), lint and build clean. Live browser verification not performed in this environment. |
| 2026-07-26 | Fixed the archive-block confirm dialog on request: once refused for pending payments, retrying without changing anything can only fail again, so the "Archive Event" button now disappears entirely and "Keep Event" relabels to "Cancel" - there is nothing left to do but back out and go resolve the payments. Added a `showConfirm` prop to the shared `ConfirmDialog` (defaults to `true`, so every other caller - EventForm, UserControlPanel, ConfigurationPanel, RegistrationPanel's refund/cancel dialogs - is unaffected) and wired both it and the relabeled cancel button to the existing `archiveError` state in `AdminDashboardPage.jsx`. Rewrote the two prior archive-block client tests (which had asserted the old retry-in-place behavior) and added a third for the Cancel-clears-the-error path; full suite green (140/140 server, 14/14 rules, 184/184 client), lint and build clean. |
| 2026-07-26 | On request ("it's getting cluttered"), reworked the admin dashboard's top area from three stacked, visually-competing rows into two clearly separated tiers. Root cause: "Needs Attention" (pill buttons) and the membership `pending-review-panel` card said the same thing twice with the same click target, and the nine-button "Manage/Edit" row gave every module identical visual weight, so nothing signaled which section was actually open. The membership card is gone; "Needs Attention" is now a single `.admin-alert-strip` of two compact chips (dot + short label + live count, e.g. "3 membership reviews pending" / "No payments need review") that read quiet-gray at zero and amber once something needs attention - one row instead of two, no duplicate sentence. "Manage/Edit" is now a real underline tab bar (`.admin-tab-nav`/`.admin-tab-button`): the active section is bold with a teal underline via both a CSS `.active` class and `aria-current="page"`, inactive tabs are muted text, and a `.admin-tab-divider` separates the day-to-day sections (Registrations through Archive) from the two admin-only ones (User Controls, Setup / System Config). The "Manage/Edit:" label itself was dropped from the desktop row - the tab bar's own `aria-label` already names it for assistive tech, and the mockup read cleaner without a redundant text prefix. Mobile is deliberately unchanged in mechanism (same `admin-nav-toggle`/`is-open` collapse) but the underline treatment reverts to the original bordered pill/teal-fill look once stacked vertically, since underlines don't read as tabs in a single column - handled entirely in the existing mobile media query, no JSX branching needed. Confirmed the direction with an HTML mockup (matching the app's real palette) before writing any code, given how subjective and hard-to-reverse-if-wrong a primary-screen layout change is. Updated `adminModuleNav.test.jsx`'s active-tab detection (from "lacks the `secondary-action` class" to "has `aria-current=\"page\"`", since the whole class scheme changed) and the divider's stray `<span>` is now excluded via `querySelectorAll('button')` instead of `.children`. Added a new `adminAlertStrip.test.jsx` (3 tests: all-clear state, pending state with correct singular/plural-verb text, and that the strip doesn't render for an admin with neither permission) - it caught a real grammar bug before this ever reached a screen: the payment chip read "1 payment need review" until the singular/plural logic was split from the noun onto the verb too. `.admin-public-nav`, `.pending-review-panel`'s standalone panel styling, and `.admin-row-label` are removed as dead code from `AdminDashboardPage.jsx`'s usage - the shared `.status-panel`/`.status-dot`/`.pending-home-card`/`.pending-review-button` classes themselves are untouched since `HomePage.jsx`, `ProfilePage.jsx`, `LoginPage.jsx`, `SignupPage.jsx`, and `UserControlPanel.jsx` still use them. Full suite green (140/140 server, 14/14 rules, 187/187 client), lint and build clean. Verified via the local dev server that the app still boots and routes cleanly with no console errors; the authenticated admin view itself needs a live Firebase sign-in this environment doesn't have. |
| 2026-07-26 | Freed up more of the same top area on request: the "Registrations" and "Payment Review" tabs are gone from the Manage/Edit row entirely for an admin who can manage events. Registrations now lives inside the Events/Activities card itself, as a second action button beside "Create New Event/Activity" (`activeModule === 'events-activities'`, gated on `canViewRegistrations`) - a plain `setActiveModule('registrations')` call, same panel as before. Payment Review has no tab at all anymore; the "Needs Attention" alert chip is its sole entry point, which already navigated there on click - only the label changed, from "No payments need review" to a plain "Payment Review" (and its dot disappears too) once the count hits zero, so a clean inbox reads as an ordinary nav item rather than a stale alert. One real access-loss risk caught while wiring this up: an admin can hold `viewRegistrations` without `manageEvents` (e.g. a registration desk role with no reason to edit events), and that admin never sees the Events/Activities card at all - so the Registrations tab still shows for that combination specifically (`canViewRegistrations && !canManageEvents`); it only disappears once the admin can also manage events and therefore has the card-button path instead. Nobody loses reachability either way. Updated the empty-state copy under "Choose what you want to manage" to point at the new locations instead of claiming Registrations lives in Manage/Edit. `.admin-list-panel-actions` gained a `gap` and `flex-wrap`, since it had never held two buttons before. Rewrote/added several `adminModuleNav.test.jsx` cases (Registrations tab present only without manageEvents, absent with it, selecting it via the Events/Activities card path, the mobile-close-on-select test switched off a tab that no longer exists) and extended `adminAlertStrip.test.jsx` (zero-state label and dot removal, pending-state dot presence, and that the chip is Payment Review's only way in). Full suite green (140/140 server, 14/14 rules, 190/190 client), lint and build clean. Live browser verification not performed in this environment. |
| 2026-07-26 | Expanded `/profile`'s read-only view on request ("show me everything") from just Name/Email/Phone/Billing Address to also include Role, Account Status, Membership Status, Membership Payment (status/amount/method), Membership Payment Updated, Member Since, and Profile Last Updated, plus a "My Registrations & Payments" link to the existing `/my-registrations` page rather than duplicating its registration/payment history here. `getDisplayAccountStatus()`/`getDisplayMembershipStatus()` in `ProfilePage.jsx` mirror the Super-User-is-always-Active-and-not-a-tracked-member exceptions already established independently in `AdminRegisterMemberPanel.jsx`, `UserControlPanel.jsx`, and `create-registration.js`'s `getProfileStatus()` - a fourth local copy, following this codebase's existing pattern of small per-file duplicates for this exact logic rather than forcing an unrelated consolidation while doing something else. The individually-granted admin permissions this row also asked for were already listed (comma-joined) in the pre-existing role-gated `status-panel` box above the detail list, so that part needed no change. Deliberately did not add a full multi-entry *membership* payment history (as opposed to the single current-payment snapshot already shown, which comes straight off the member's own `users` document and was therefore already safely readable): `firestore.rules`' `payments` collection currently lets a signed-in non-admin read only `entityType == 'Registration'` docs they own, not `'Membership'` ones, so a full history would need a new rule - flagged for a follow-up decision rather than opened silently. 4 new client tests (full detail rendering with realistic values, defaults when status/payment fields are unset, the Super User exceptions holding with no status set at all, and the registrations link's href); full suite green (140/140 server, 14/14 rules, 194/194 client), lint and build clean. Live browser verification not performed in this environment. |
| 2026-07-26 | Added the full membership payment history flagged in the entry above, confirmed worth doing. New `firestore.rules` branch on `payments`: `signedIn() && resource.data.entityType == 'Membership' && resource.data.entityId == request.auth.uid` - membership payments carry no `userId` field, only `entityId` (set to the member's own `users/{uid}` doc ID by `buildMembershipPaymentRecord()` in `admin-update-user-profile.js`), which is why this couldn't just reuse the existing Registration-type branch. New `subscribeToMembershipPayments()` in `registrationService.js` queries on that same `entityType`/`entityId` pair, matching the rule exactly. `ProfilePage.jsx` subscribes on mount and renders a `MembershipPaymentHistoryList`, a near-verbatim port of `UserControlPanel.jsx`'s admin-side `MembershipPaymentHistory` (same `payment-history-list`/`payment-history-item` classes, so no new CSS), sorted newest-first. 5 new Firestore rules tests in `tests/rules/payments-self-read.test.js` (own membership payment readable, another member's refused, signed-out refused, and two regression guards confirming the pre-existing Registration-type self-read branches still work unaffected by the new one) and 2 new client tests (empty state, and newest-first ordering with amount/recorded-by rendering correctly) - the ordering test deliberately supplies the newer record second, so it would fail if the sort were ever silently dropped. Full suite green (140/140 server, 19/19 rules, 196/196 client), lint and build clean. Requires `firebase deploy --only firestore:rules` before this is readable in production - until then, a member's Membership Payment History section will show empty even though records exist, the same silent-until-deployed gap prior rules-only changes in this project have had. Live browser verification not performed in this environment. |
| 2026-07-26 | Raised the per-event photo limit from 1 to `MAX_EVENT_IMAGES` (4), on request ("I often get two or three photos... but I only do one because of space"). The multi-image plumbing already existed in `EventForm.jsx` end to end - index-based upload/remove handlers, per-slot Firestore Storage paths - it had just been deliberately capped to a single rendered slot and a `.slice(0, 1)` on save; raising the cap only meant changing a hardcoded `1` to the new shared constant in three places (the render loop, the save slice, the edit-load array) rather than building new upload infrastructure. Confirmed the display approach with the user before writing any code: one photo visible at a time with prev/next arrows, never a multi-image strip. New `EventImageCarousel.jsx` is a drop-in replacement for the old single-image-or-placeholder ternary - identical output for 0 or 1 images, confirmed live against the real dev database on `/events` and `/business-listings` (existing single-image test events render unchanged, no console errors) - and only shows CSS-drawn arrows and dot indicators once an event actually has 2+ photos. Wired into the three member-facing card locations (`EventsPage.jsx`, `EventDetailsPage.jsx`, `PublicListingPage.jsx`); the admin management thumbnail, the print views, and the third-party GoDaddy feed all intentionally keep showing just the first photo. Added a small `getEventImagesError()` alongside the existing `getEventCapacityError()` as a server-side defense-in-depth cap on this authenticated write path. 14 new client tests and 2 new server tests; full suite green (142/142 server, 19/19 rules, 210/210 client), lint clean apart from two pre-existing-pattern warnings from exporting two EventForm.jsx helpers for direct testing, build clean. |
| 2026-07-26 | Fixed real-world image uploads failing with `storage/unauthorized` right after the entry above shipped, reported against both a Super User and a separate ordinary Admin account. Root cause was in `storage.rules`, not the new upload code: a 2026-07-24 change (commit message literally `"sdgsdf"`, unreviewed) had added a `canManageEventFiles()` check gating `event-images`/`event-documents` writes on a cross-service `firestore.get()` reading the uploader's role and status from Firestore before allowing the write. That call denied every account in production despite those same accounts working fine everywhere else in the app - a first pass at a fix (built and shipped) added a comment-only diff so the Firebase Console would treat the file as changed and allow a re-publish, on the theory the live rules had drifted from the repo; the user re-tried with a second, different Admin account afterward and hit the identical error, which ruled that out too, since the Console had confirmed the rule text was already live and unchanged. Built a local Firestore+Storage emulator reproduction of the exact committed rule text (`initializeTestEnvironment` with both `firestore` and `storage` blocks) and found it authorized a Super User with no `permissions` map, a Super User with an empty one, and an Admin with `manageEvents: true` correctly, every time - the same rule text that was failing for two different real accounts in production. Two identical accounts failing where two different account shapes both passed locally pointed at the cross-service read itself, not account data, so reverted `canManageEventFiles()` entirely rather than keep guessing at production-only Firebase behavior with no console/log access to verify against. `event-images`/`event-documents` are back to the ownership-plus-size/type check (`ownsFolder(userId) && validImageUpload()`/`validPdfUpload()`) this project ran on for weeks before the July 24 change - Firestore's own rules already keep event documents server-write-only regardless of what Storage allows, so this doesn't loosen the actual data model, only who can drop a file into their own uid-named folder. Added this project's first Storage rules test suite (`tests/rules/storage-event-files.test.js`: own-folder upload succeeds, cross-folder upload refused, signed-out refused, wrong content-type refused, reads stay public) and updated `test:rules`/CI to boot the Storage emulator alongside Firestore's, so a future re-introduction of a broken cross-service check here gets caught by a real account scenario instead of only being checked by eye. 24/24 rules tests green (5 new). Could not deploy or verify against the live Firebase project directly - deploy access belongs to the user; asked them to re-test after publishing. |
| 2026-07-26 | Once uploads were confirmed working, added auto-rotation with a pause toggle plus a "N Photos" caption on request. `EventImageCarousel.jsx` now advances to the next photo every 4 seconds when there are 2+ images, via a `setInterval` re-created on every `index` change (including manual arrow clicks) so a manual click always gets a full fresh interval before the next auto-advance rather than one that could land moments later. A CSS-drawn pause/play toggle (matching the existing chevron-arrow drawing technique, no icon library) lets a viewer stop it - not just a nicety, WCAG 2.2.2 requires a way to pause any auto-updating content. The photo-count caption is separate from the rotation logic and shows for any event with at least one photo, including the single-image case (a deliberate, requested change to the "renders identically for 0/1 images" contract from the entry that added this component). Added the same caption to two more places on request: the admin `EventList.jsx` thumbnail (still a static first-image display there, no rotation - just the count, imported as `formatPhotoCount()` from the carousel component to avoid a second copy of the pluralization) and the GoDaddy feed (`api/_lib/public-event-feed.js`, new `imageUrls`/`imageCount` fields alongside the existing single `imageUrl` field it already exposed) - flagged to the user that GoDaddy's widget previously only ever read the single `imageUrl` field, so whether their template actually displays the new fields depends on their side, not this app's. Verified live against the real dev database after the earlier storage fix: a real event with 4 uploaded photos auto-advanced through all four (confirmed via the active dot index after a 5-second wait), the pause toggle correctly froze it in place for another 5 seconds, and the "4 Photos" caption and all 4 dots rendered correctly - no console errors. 4 new client tests for the carousel (pause toggle absent for ≤1 image, auto-rotates and wraps via fake timers, pause/resume via fake timers, a manual click resets the interval rather than double-advancing), 3 new client tests for the `EventList` caption, and 2 new server tests for the feed's new fields; full suite green (144/144 server, 24/24 rules, 217/217 client), lint clean apart from one more instance of the same accepted `EventImageCarousel.jsx` export-for-sharing warning, build clean. |
| 2026-07-26 | Brought the GoDaddy embed script's own card thumbnails and "larger view" popup up to the same carousel behavior as the main app, on request - the feed's `imageUrls`/`imageCount` fields from the entry above had shipped, but `public/godaddy-event-feed.js` itself, a dependency-free vanilla-JS IIFE with no build step and no access to the React component, was still rendering only the first photo with no way to see the rest. `buildThumbnailMarkup()` now branches on photo count: 0 stays a placeholder, 1 keeps the existing single-image button (now also carrying `data-images`/`data-title` so the popup path below can use it), and 2+ renders a `.vq-feed-carousel` container - prev/next arrow buttons, a pause/play toggle, and dot indicators - all wired through a new `wireImageCarousels()` using `data-action`/`data-role` attribute delegation (the same array-in-a-`data-*`-attribute-plus-`JSON.parse()` pattern this file already used elsewhere for its coordinator/event-details data), mirroring `EventImageCarousel.jsx`'s 4-second auto-rotate-with-pause behavior in plain DOM calls since there's no React state here. Clicking the thumbnail (or the old single-image link) now opens a popup that also carries a full carousel when the event has 2+ photos, not just a static image: `openImageViewer(imageUrl, title)` became `openImageViewer(images, title, startIndex)`, carrying over the card's current photo as the popup's starting index, and its `document.write()`'d HTML gained its own self-contained `<style>` and inline `<script>` (a popup is a separate window/document, so it can't share the parent page's closures) reimplementing the same prev/next/pause/dots/auto-rotate logic independently. Verification needed a workaround since this script has zero existing test coverage and there's no live GoDaddy site to test against: built a throwaway static HTML fixture (mounted at `data-vq-feed`, matching the script's actual `mountSelector`) with a stubbed `fetch` standing in for `/api/public-events`, loaded through the Vite dev server's static file serving. Confirmed live in the browser: the multi-photo card auto-rotates and wraps on next/prev, the pause toggle halts rotation for a full interval and resumes correctly, and the dots track the active index. `window.open` itself is blocked outright in this sandboxed browser environment (true for the pre-existing single-image path too, not a regression), so the popup was verified by intercepting `window.open` to capture the exact HTML `openImageViewer()` writes and rendering that captured markup in an iframe - confirming the popup's independent carousel script correctly reflects the card's starting index, advances via its own arrows, and its own pause toggle halts its own auto-rotate interval, all isolated from the parent page. No test suite covers this file (matching its pre-existing zero-coverage state); full suite otherwise green (144/144 server, 24/24 rules, 217/217 client), lint and build clean. |
| 2026-07-27 | Added a coordinator email notification for every new registration, on request, with two decisions confirmed up front: coordinators already hold admin accounts with `viewRegistrations` (so the email's list link can just deep-link into a normal authenticated admin route rather than needing a new signed-link mechanism this codebase has no precedent for), and it fires once per registration event - including waitlist joins, not just confirmed seats. Fires from the same post-commit, non-blocking insertion point in `api/create-registration.js` as the existing registrant confirmation email, wrapped in the same `withTimeout`/`.catch` pattern, and gated by its own new `sendCoordinatorRegistrationNotifications` toggle in Configuration's Email Instructions card - deliberately independent of the existing "Send Confirmation Emails" toggle, since a guild might want one on without the other for a busy event. Coordinator lookup reuses the existing per-area (Programs/Workshops/Challenges) `getCoordinatorContact()`/`getEmailInstructionArea()` already backing the registrant email; skips sending entirely if that area has no coordinator email configured. New `computeRegistrationSummary(event, registrations)` in `api/_lib/registration-capacity.js` mirrors `RegistrationPanel.jsx`'s own `reduceRegistrationCounts`/`getTotalPaidAmount` bucketing exactly (registered/waitlisted/cancelled/pending-payment counts, capacity/seats-available, total paid summing only `paymentStatus === 'Paid'`) so the email's numbers always agree with what an admin sees in the Registrations card - computed server-side only, since that component is a React file `api/` can't import from. The email queries `registrations` fresh by `eventId` after the transaction (and, for the Square online-card path, after its payment finalizes) rather than reusing the pre-transaction snapshot, so the stats reflect everything actually committed by send time. Added a "Print Registration List" button (both in the email and, per a same-day follow-up request, directly on each event's card in both the Registrations panel and the Manage Events list) linking to a new `/admin/events/:eventId/registrations/print` route (`RegistrationListPrintPage.jsx`) - a real page reachable by direct link rather than `ArchivePanel.jsx`'s existing `window.open`/`document.write` print report, since that one only works from in-memory SPA state already on screen, not a cold link from an email. Deliberately lists every registration status including Waitlisted (the explicit ask), reusing `.viewer-page`/`.viewer-toolbar` (already hidden correctly under the site's `@media print` rules) and the same `.registration-event-stats`/`has-count`/`no-count` pill classes and `.user-table.registration-table` markup `ArchivePanel.jsx` and `RegistrationPanel.jsx` already use, so no new CSS was needed. Gated on the existing `viewRegistrations` permission read from the signed-in admin's own profile via `useAuth()`, on top of the route's existing `RequireAdmin` wrapper. `getRegistrationsForEvent(eventId)` (new, `registrationService.js`) is a one-time `getDocs` query rather than a live subscription, matching this page's one-shot print-and-close nature; Firestore rules already enforce `viewRegistrations` per-document for this exact query pattern, so no new rule was needed there. The settings toggle did need a rules change: added `sendCoordinatorRegistrationNotifications` to `validEmailInstructions()`'s `hasOnly()` whitelist and `is bool` check - an easy field to get subtly wrong (a typo'd key or missing type check would reject every future save of this settings doc, or silently accept a bad value), so added this project's first rules coverage for `appSettings/emailInstructions` (5 tests) specifically for the new field rather than leaving it as untested as the rest of that document. 6 new server tests (`computeRegistrationSummary`), 8 new client tests (the new print page's stats/waitlist rendering and permission gate, plus the new card-level print links on both `EventList.jsx` and `RegistrationPanel.jsx`'s existing card action rows), 5 new rules tests; full suite green (150/150 server, 29/29 rules, 225/225 client), lint and build clean. Requires `firebase deploy --only firestore:rules` before the new settings field can be saved in production. Live verification was limited to confirming the new route resolves cleanly through `RequireAdmin` (redirects to sign-in with no console errors) via the local dev server - actually sending the email (requires `RESEND_API_KEY` and a live Firestore project) and viewing the print page as a signed-in admin could not be exercised in this environment. |
| 2026-07-27 | User confirmed the coordinator notification email from the entry above now works live, after re-enabling the new toggle they'd initially missed. Three follow-up requests from that same test: added a Phone column to `RegistrationListPrintPage.jsx`'s registrant table, alongside the existing Registrant/Registered/Status/Payment columns. Changed `MyRegistrationsPage.jsx`'s default sort from primarily-by-event-date (registration date only as a tiebreaker) to purely registration-date descending, matching the "newest signup first" convention this project already settled on for the admin registrant table (see the 2026-07-26 entry on admin-initiated registration's third follow-up) - removed the now-dead `getRegistrationEventSortValue()` helper the old comparator used. Renamed the "Amount Due" label to "Amount" across all four registration email templates (`buildRegistrationConfirmationHtml`/`Text` and the new `buildCoordinatorNotificationHtml`/`Text` from the entry above) for consistency, after first trying "Cost" per the initial request and then landing on "Amount" as the actual final wording, both in the same conversation. One new client test for the sort order (`myRegistrationsSort.test.jsx`) caught a real bug in itself before it caught anything in the app: mocking `useAuth()` to return a fresh `{ currentUser: {...} }` object literal on every call gave `MyRegistrationsPage`'s `[currentUser]`-keyed effect a "changed" dependency on every render (the effect's own subscription callback calls `setState` synchronously), which spun into an infinite render loop that pegged a test worker at 100% CPU indefinitely rather than failing - not caught by the normal test timeout because vitest's per-test timeout doesn't preempt a synchronous-looking render loop the way an async hang does. Fixed by hoisting the mock's auth state to a stable module-level object, matching the pattern this project's other `useAuth()`-mocking tests (e.g. `profilePageEditing.test.jsx`) already use for the same reason. 2 new client tests total (the sort order, plus updating `registrationListPrintPage.test.jsx`'s existing table assertions incidentally still passing with the new Phone column); full suite green (150/150 server, 29/29 rules, 226/226 client), lint and build clean. |
| 2026-07-27 | Fixed the "Print Registration List" links opening in a new tab, reported right after the previous entry's live test - both the `EventList.jsx` and `RegistrationPanel.jsx` action-row links used a plain `<a target="_blank">`, matching the earlier assumption that a coordinator reading it from an email would want a separate tab, but that same link now also appears as an ordinary in-app admin action, where a new tab is surprising. Converted both to `react-router-dom`'s `Link` (dropping `target`/`rel` entirely) so the print page opens in the same tab via normal SPA navigation instead - `RegistrationPanel.jsx` had never imported `react-router-dom` before, so this is a new import there. `RegistrationListPrintPage.jsx`'s own Close button needed no change: its existing `window.opener` branch (for the popup case) simply goes unused now, falling through to the `window.history.back()` branch, which is exactly right for same-tab navigation. Since `RegistrationPanel.jsx` now renders a real `Link`, its one direct-render test file (`registrationPanelArchiveFilter.test.jsx`) needed a `react-router-dom` mock added (it previously got away without one, because nothing in that component touched routing until now) - the four other test files that exercise this component do so through `AdminDashboardPage.jsx` with `RegistrationPanel.jsx` itself mocked out, so they were unaffected. Also dropped a `target` assertion from `eventListTotalPaid.test.jsx`'s print-link test that the same-tab change made incorrect. Full suite green (150/150 server, 29/29 rules, 226/226 client), lint and build clean. |
| 2026-07-27 | Restyled the main site header on request ("smaller modern... breadcrumbs or back buttons... add my profile name along with the email in the status bar"), confirmed against a mocked-up before/after comparison before writing any code, given how visible and hard-to-reverse-if-wrong a primary-screen layout change is. The old two-strip chrome (an 18px-padded `.site-header` plus a separate full-width `.auth-banner` showing only the Firebase Auth email) is now one `.site-header`: a 34px logo, nav links restyled from 8px-radius boxes into true pills (`border-radius: 999px`, unfilled by default, teal-filled only when active) matching the pill language already used elsewhere in the app (pull-to-refresh strip, role badges), and a new `.site-identity` chip carrying an initials avatar, the signed-in member's name (bold) above their email (muted) - the explicit ask - plus the Admin/Signed-in role pill and a Sign out link, replacing the old banner entirely. Splitting `.nav-link`/`.nav-button` out of the shared `.button-link` rule block they'd been co-defined in since the start of the project was the trickiest part: `.button-link` is used for dozens of ordinary buttons app-wide and had to come out with its exact prior appearance (8px radius, teal fill, `.secondary-action` outline variant) untouched, while only the header's own nav items got the new pill treatment - verified by grep that `.nav-button` had never been used for anything but the header's old Sign out button, so it was retired outright rather than kept as dead CSS. A real layout bug surfaced during live verification, not by lint or tests: at browser-pane width (~800px), the identity chip's fixed ~310px width squeezed the nav row down to 288px, wrapping five nav pills onto three cramped lines instead of one or two clean ones. Fixed by giving `.site-header` `flex-wrap: wrap` and assigning explicit `order` values (brand 1, identity 2, nav 3 at `flex: 1 1 100%`, menu button 4) so nav is always forced onto its own full-width second row below brand+identity, rather than left to fight the identity chip for space on a single line - a small, deliberate two-row header rather than an unpredictable wrap. `getAccountDisplayName(currentUser, userProfile)` (new, `src/utils/profileFormat.js` - name lives on the Firestore profile, which starts `null` until it loads, so it falls back to the Firebase Auth account's own `displayName`, set at profile save time, so a name still shows in the gap right after sign-in) is shared by both the new desktop chip and `MobileNavSheet.jsx`'s existing account card, which also gained the name (its own `.nav-sheet-account-name`/`-email` styles rebalanced so the name reads primary and the email secondary, matching the new desktop hierarchy). Second, added a lightweight breadcrumb: a new optional `breadcrumb` prop on `PageHeader.jsx` (an array of `{ label, to? }`, the last entry or any entry missing `to` rendering as plain text) wired into `EventDetailsPage.jsx` ("Programs & Activities > Event") and all three `RegisterPage.jsx` states that have full event context ("Programs & Activities > Event > Register" / "> Registration Complete"), replacing the ad hoc "Back to events" link on the former. Deliberately left the print/supply-list/registration-list pages alone - they render in this app's existing chromeless "popup mode" with their own Print/Close toolbar already, a different and already-settled navigation idiom, not a gap. 4 new tests for `getAccountDisplayName`, 4 for `PageHeader`'s breadcrumb rendering (including a mid-trail entry with no `to`), and 2 more for `MobileNavSheet.jsx`'s account card gaining a name; full suite green (150/150 server, 29/29 rules, 236/236 client), lint and build clean. Verified live in the browser at three viewport widths (375/800/1280px) as a real signed-in Admin account already active in this environment's session: the identity chip, pill nav, and both fixed layout bug and its correction, the mobile sheet's name row, and all three breadcrumb levels (event details, register form, and the register-flow's link back through the event) all rendered and linked correctly with no console errors. |
| 2026-07-27 | Follow-up from the first live look at the header above: the nav pills "take up a lot of room" - replaced with plain text links, centered on their row. `.nav-link` dropped its `background`/`border`/`border-radius`/`min-height` entirely (no more pill box), padding down to `6px 2px`, and the active state changed from a teal fill to teal text with an underline (`text-underline-offset: 5px`) so it still reads at a glance without needing a background. `.site-nav` gained `justify-content: center` and a wider gap (`8px 26px`, up from `6px`) - text links sitting directly next to each other with no visual boundary need more breathing room than pills did. The two-row layout and `order` fix from the entry above (nav forced onto its own line below brand/identity) is untouched and still does its job now that the row is shorter. Verified live at 800px and 900px: five links centered, active link (Home) underlined in teal, no wrapping. Client suite still green (236/236, unaffected - no test asserted the pill background/box), lint and build clean. |
| 2026-07-27 | Reordered the header nav to Home, Programs/Activities Signup, My Registrations, Member Directory, My Profile, on request. Login (only ever visible signed out, when Programs/Activities Signup and the rest of the signed-in-only items are hidden anyway) kept its slot right after Home rather than being dropped from the array, since it wasn't named in the requested order and the two states never render side by side. Single-array change in `App.jsx`'s `navDestinations` - both the desktop row and `MobileNavSheet.jsx` read from the same list, so the mobile sheet picked up the new order automatically. No test asserts nav order (mobile sheet's own tests use a local fixture list, not the real one), so nothing else needed updating. Verified live: both desktop and the mobile sheet show the new order. Full suite green (236/236 client), lint and build clean. |
| 2026-07-27 | Two more header follow-ups from live testing: restored the "Managing events, activities and members" tagline under the brand name (dropped in the original restyle, on request to bring it back), and tried putting the text nav on the same row as the logo and identity chip, centered, rather than its own row below - both explicitly requested. The tagline was a straightforward JSX/CSS revert. The single-row nav attempt surfaced a second real layout bug, this time from over-trusting flexbox's automatic wrapping for a three-group header (logo left, nav center, identity right): giving nav `flex: 1 1 auto; min-width: 0` and letting the header's own `flex-wrap` bail it out when things didn't fit re-created the exact squeeze from two entries ago - at 800px wide, nav was left only ~104px (measured) between the brand and identity boxes and folded into a cramped 232px-tall stack instead of dropping to a clean second line, because flexbox packs items in a fixed order and can't reshuffle "nav drops alone" vs. "nav sits between brand and identity" from one `order` scheme - those are two different desired arrangements that don't both fall out of the same automatic wrap. Replaced the automatic approach with an explicit, measured breakpoint: temporarily set nav to `flex-wrap: nowrap` in the console and read its `scrollWidth` (653px) to get its true single-line content width, rather than guess from a couple of sample viewport widths - brand (~270px) + nav (653px) + identity (~310px) + gaps/padding only clears one line above roughly 1400px. Below `1400px` (including the 760-1400px range that broke above), nav stays forced onto its own full-width centered line under brand/identity (`order: 3`, `flex-basis: 100%`) exactly as fixed two entries ago; a new `@media (min-width: 1400px)` block reassigns `order: 2` to nav and `order: 3` to identity, letting nav grow to fill the space between them on one row once there's actually room. Verified live at five widths this time instead of one or two: 800px and 1280px both show the clean two-row fallback (no cramping), 1500px and 1600px show the single row with nav genuinely centered between logo and identity, and 375px (mobile) is unaffected. Client suite still green (236/236 - no test asserts header row count or the breakpoint), lint and build clean. |
| 2026-07-27 | Fixed a CI-only rules test failure the user hit right after pushing (`payments-self-read.test.js`, "the existing registration-payment self-read path still works" - unrelated to the header work above, which touches no rules or backend code). It passed locally on the first three re-runs, which pointed at an environment difference rather than a real logic bug at first glance - CI's job log showed it downloading a fresh `cloud-firestore-emulator` jar every run (no cache), while the local machine reuses whatever version is already cached, so the two were plausibly evaluating the rule differently. The actual bug in `firestore.rules`'s `payments/{paymentId}` read rule: the registration-payment-by-registrationId branch checks `resource.data.registrationId is string` before calling `get(/databases/.../documents/registrations/$(resource.data.registrationId))`, but an empty string still satisfies `is string` - the test fixture's `registrationPayment()` has `registrationId: ''` by design (it's read via the earlier direct-`userId` branch instead), so that later branch was attempting `get()` on `registrations/` with an empty path segment, an invalid document reference. Firestore's rule evaluator is documented to short-circuit `||`, so with the earlier userId-match branch already true this should never have run at all - evidently that guarantee isn't identical across emulator versions, which is exactly why one environment's cached jar let it slide and a freshly-downloaded one didn't. Fixed at the source rather than relying on short-circuit behavior to keep saving it: added `&& resource.data.registrationId != ''` alongside the existing `is string` check, so the invalid `get()` is never reachable regardless of evaluation order or emulator version. Added a new test pinning exactly this fixture shape (`registrationId: ''`) so a future edit that drops the empty-string guard fails deterministically instead of depending on which emulator jar happens to be cached; 30/30 rules tests pass, re-ran three times locally to build confidence given the original flakiness. Full suite otherwise unaffected (150/150 server, 236/236 client), lint and build clean. |
| 2026-07-27 | The empty-`registrationId` guard from the entry above turned out to be a real bug worth fixing but not the cause of the CI failure it was written for: the user reported the identical failure again, on the identical test, after that fix shipped - confirmed via `gh run view --log-failed` on the new run, which showed the exact same "evaluation error ... Null value error" on the exact same assertion, even though a second new test covering the empty-string fixture passed cleanly in that same run. Two structurally-identical `assertSucceeds` calls in the same file, one failing and one passing, in the same CI run, ruled out fixture content as the variable. Reproduced the actual cause locally by isolating variables: `payments-self-read.test.js` alone, run five times, never failed; the full `tests/rules/*.test.js` glob, run five times locally, never failed either - but every `*.test.js` file opens its own `initializeTestEnvironment()` against the same live Firestore emulator (same host, same `vq-rules-test` project), and each test's `seed()` helper calls `testEnv.clearFirestore()` before writing its fixture. Node's test runner runs separate test *files* concurrently by default (only tests within one file run in sequence) - so one file's `clearFirestore()` can wipe the exact document another file's in-flight rule evaluation is mid-read on, and a `get()` hitting a document that vanishes mid-evaluation is exactly what a Null-value evaluation error looks like. This didn't reproduce locally because the race needs the right timing, and CI's slower/more constrained runner apparently hits that window far more reliably than a local machine did across eight total local attempts. Fixed by adding `--test-concurrency=1` to `test:rules`'s `node --test` invocation, forcing every rules test file to run one at a time against the shared emulator instead of racing each other - the actual structural bug, not a timing coincidence to work around. Verified with 3 more local runs post-fix (all 30/30) on top of the 8 that already couldn't reproduce it; the true test of this fix is the next CI run, which the user will see once they push. Full suite otherwise unaffected (150/150 server, 236/236 client), lint and build clean. |
| 2026-07-27 | Moved "My Profile" out of the main nav row into the identity chip/account card, on request, since "the login icon is in its own pill" already had room for it and freed up space on the row that wraps below 1400px. Desktop: `navDestinations` no longer includes `/profile`, and `.site-identity` gained a small `My Profile | Sign out` action pair (new `.site-identity-actions`/`.site-identity-action`/`.site-identity-divider` styles, replacing the old single-purpose `.site-identity-signout` class with a shared one both links/buttons use). Mobile: since `MobileNavSheet.jsx` reads from the same `navDestinations` array, removing `/profile` there too would have dropped it from the phone menu entirely - added a matching `My Profile` link directly inside the account card instead (`.nav-sheet-account-profile-link`), so both surfaces keep the same "profile lives with your identity, not with navigation" shape. Updated `mobileNavSheet.test.jsx`'s destination fixture and its "lists every destination" assertion to drop `/profile` (it no longer reflects what `App.jsx` actually passes) and added 2 new tests: the account-card link resolves to `/profile` and lives inside `.nav-sheet-account` rather than being a `.nav-sheet-row`, and selecting it closes the sheet like every other action does. Verified live at 1000px (desktop chip shows "My Profile | Sign out", nav row now down to 4 links) and 375px (mobile sheet's account card shows the link, destination list no longer repeats it). Full suite green (150/150 server, 238/238 client), lint and build clean. |
| 2026-07-27 | Added PDF supply-list upload support for the "Other" event type, on request. The gating for this feature turned out to already be a single source of truth: `EventForm.jsx`'s private `supportsSupplyList(eventType)` (`eventType.startsWith('Class') \|\| eventType === 'Workshop'`), which every consumer - the render gate for the upload UI, the field-clearing logic when an admin switches event type away from a supported one, and `buildEventPayload()`'s field-stripping on save - already derived from, so adding `\|\| eventType === 'Other'` was the entire functional change; no other file needed touching. `uploadEventPdf()`/`storage.rules`'s `event-documents/{userId}/{fileName}` path are both already type-agnostic (ownership + content-type only), and every public display surface (`EventDetailsPage.jsx`, `EventsPage.jsx`, `SupplyListViewerPage.jsx`, `public/godaddy-event-feed.js`, `api/_lib/public-event-feed.js`'s `serializeEvent`) gates purely on `event.supplyListUrl` being present, not on event type - so the uploaded PDF will display correctly once an Other event has one, with no changes needed there either. Exported `supportsSupplyList` (previously private) following this file's established pattern of exporting pure helpers for direct testing rather than only exercising them through a full component render. Flagged to the user but deliberately left unaddressed, since it's a separate and materially larger scope than "add PDF upload": `'Other'` events are not included in any of `EventsPage.jsx`'s `EVENT_TYPE_FILTERS` groups or `public/godaddy-event-feed.js`'s filter tabs, so an `'Other'` event (PDF or not) does not currently surface on the public `/events` listing's Programs/Workshops/Challenges tabs or the GoDaddy widget's default view - it's still reachable by direct link (`EventDetailsPage.jsx` has no such filter) and via the raw API feed. 6 new tests (`supportsSupplyList` for every event type category, and `buildEventPayload`'s field-stripping for both a supported and unsupported type); full suite green (150/150 server, 243/243 client), lint clean apart from one more instance of the already-accepted `EventForm.jsx` export-for-testing warning, build clean. Verified live against the real dev database as a signed-in Admin: selecting "Other" shows the Supply List Upload field, switching to "Retreat" hides it again, switching back to "Other" shows it again - no console errors. |
| 2026-07-27 | Added a phone-lookup option to the login page's "Forgot password or username?" panel, on request: enter the phone number on your account, get emailed a 6-digit code (reusing the registration flow's email-code pattern - hashed at rest, 10-minute expiry, rate-limited by IP and by phone), and once verified the account's email address is revealed. New `api/_lib/account-recovery.js` and a standalone `api/account-recovery.js` endpoint. Immediately hit the Vercel Hobby plan's 12-function cap (confirmed via the deploy error), so the endpoint was folded into the existing `registration-lookup.js` action router instead of staying a separate file - the same fix pattern this row keeps coming back to. Also fixed the phone number not live-formatting while typing, and changed the password-reset confirmation copy from "Firebase will send..." to "we will send...", both on request. |
| 2026-07-27 | Redesigned the recovery flow again on request ("just want the user to send in either their email or phone and if they have an account they would get a code emailed... then log them in"): collapsed the phone-only panel into one field accepting either email or phone, and a verified code now signs the member in for real (a Firebase custom token) and lands them on `/profile` with the edit form already open and the password field focused, rather than just revealing the email address. Shipped with a real production defect: `getAuth(app).createCustomToken()` from `firebase-admin/auth` crashed the whole endpoint (`FUNCTION_INVOCATION_FAILED`) on every invocation, because that subpath breaks Vercel's function bundling - this codebase already avoids it entirely (`api/_lib/firebase-token.js` hand-verifies ID tokens with `node:crypto` instead), so the fix was to hand-sign the custom token JWT the same way, with a test that generates a real RSA keypair and cryptographically verifies the output signature. Also restored live phone-number formatting on the merged field (only while the input looks like digits, so typing an email is never reformatted), fixed a CI failure from `ProfilePage`'s new `useSearchParams` import missing from a test's `react-router-dom` mock, and added the missing explicit deny-all Firestore rule for the new `accountRecoveryVerifications` collection to match its sibling collections. Two more follow-ups on request: the email/password fields and Sign In button now hide entirely while the recovery panel is open (only one thing to fill in at a time), and the "Not a Guild member?" link text was updated. The generic "we sent a code" message now states the 10-minute expiry, and a successful recovery sign-in auto-opens the profile edit form (waiting for `userProfile` to load first so fields are populated before the form reveals itself) in addition to focusing the password field - both on request. |
| 2026-07-27 | Included the "Other" event type under the Programs filter on both `/events` (`EventsPage.jsx`) and the GoDaddy embed widget (`public/godaddy-event-feed.js`), on request - the exact follow-up flagged as deliberately unaddressed in the PDF-supply-list entry above. Same root cause in both places: a type-classification function (`EVENT_TYPE_FILTERS`'s `Programs` bucket client-side, `isProgramType()` in the widget) that every other non-excluded type had an entry in, except `Other` - so those events weren't just missing a filter tab, they were silently dropped from the entire listing (`registerableEvents` requires a matching filter) or permanently hidden behind a filter that could never match them (the widget's only two tabs are Programs and Workshops). Added `'Other'` to both. `dist/godaddy-event-feed.js` needed no manual edit - confirmed it's a straight `public/` copy that Vite regenerates on build. |
| 2026-07-27 | Added a waitlist management module, on request, following a design discussion first: the user asked whether a freed seat should auto-register the next waitlisted person or notify a coordinator to move them manually, and agreed with the recommended third option - auto-email the next person an offer to claim the seat themselves (no auto-charge), with the coordinator looped in only if the whole waitlist is exhausted. `isSeatHoldingRegistration()` (`api/_lib/registration-capacity.js`) now also treats a Waitlisted registration with an unexpired offer as seat-holding, which is the entire "reserve the seat during the claim window" mechanism - no parallel reservation collection. New `api/_lib/waitlist.js` holds `createNextWaitlistOffer()` (queries the event's Waitlisted registrations ordered by `registrationDate`, skips anyone already offered, re-verifies capacity, and either emails the next candidate or the coordinator if none remain) and `expireStaleWaitlistOffers()` (the daily cron sweep, cascades to the next person on an unclaimed expiry). The claim window is 3 days rather than 48 hours because Vercel's Hobby plan only runs cron jobs once a day - a tighter window couldn't actually be enforced at that check frequency, so the copy doesn't promise precision the infrastructure can't deliver. New `claimWaitlistOffer`/`manuallyPromoteWaitlisted` actions and a `CRON_SECRET`-gated `GET` handler were added to the existing `create-registration.js` action router (not a new file) to hold the Hobby-plan function count at 12; the claim page (`src/pages/WaitlistClaimPage.jsx`, route `/waitlist-claim`) is a magic-link flow requiring no sign-in, consistent with the account-recovery redesign above. A real, unrelated bug was caught while extending capacity logic: two existing call sites did `existingRegistrations.filter(isSeatHoldingRegistration)`, and `Array.prototype.filter` passes the array index as a callback's second argument - which `isSeatHoldingRegistration`'s new `now` parameter would have silently received instead of a timestamp, making every waitlist offer (even long-expired ones) permanently read as still holding a seat. Fixed both call sites to pass the registration alone. New Firestore fields (`waitlistOfferedAt`, `waitlistOfferExpiresAt`, `waitlistOfferTokenHash`, `claimedDate`) are admin-SDK-only writes, so no rules change was needed; a new composite index (`registrations`: `status`, `waitlistOfferExpiresAt`) was added for the cron's cross-event sweep and deployed live via `firebase deploy --only firestore:indexes` (the user's own `firebase-tools` login had to be re-authenticated first; `firebase-tools` turned out to already be a project devDependency, just not installed as a global command). `CRON_SECRET` still needs setting in the Vercel dashboard by the user - covered separately, not part of this repo's code. Full local suite green (170 server, 243 client, 30 rules), lint and build clean; live email/admin-panel/Square verification not performed in this environment (same standing caveat as every email-sending feature). |
| 2026-07-27 | User tested the shipped waitlist flow live and reported the admin Registrations card showed their own membership as "Unknown" instead of Active. Root cause was unrelated to the waitlist work: `RegistrationPanel.jsx`'s `subscribeToUsers(..., { includeAdminProfiles: false })` excludes Admin/Super User profiles from the membership lookup, so an admin's own registration could never resolve a status for their own account. Fixed by switching to `includeAdminProfiles: true`, matching how `UserControlPanel.jsx` already needs every profile to do its job. |
| 2026-07-27 | That fix immediately broke navigation for non-Super-User admins with "Missing or insufficient permissions", reported right after clicking the new "Review/Edit Registrations" shortcut. `firestore.rules`' `users` collection only lets a plain Admin read docs where `role == 'General User'` - a Super User is required to read the collection unfiltered, and Firestore denies a list query outright if any admin/super-user document in the result set fails that check for the requester. `UserControlPanel.jsx` already handles this correctly (`{ includeAdminProfiles: canManageAdminUsers }`, i.e. `isSuperUser`); `RegistrationPanel.jsx`'s blanket `true` from the entry above skipped that gate. Fixed by threading `isSuperUser` down from `AdminDashboardPage.jsx` and using it the same way. Tradeoff stated to the user rather than silently reverted: a plain Admin will again see "Unknown" for another admin/super-user's own registration, since that's a real Firestore security boundary (a plain Admin cannot read another admin's profile document), not an oversight - widening it further would mean loosening who can read admin-level profile data, a separate decision. |
| 2026-07-27 | Added "Review/Edit Registrations" and "Register A Member" shortcuts to each event card, on request, so managing an event's registrations no longer requires leaving Events/Activities, opening the separate Registrations module, and finding that event again in its own list. Both jump straight into the Registrations module with the event pre-selected via lifted state in `AdminDashboardPage.jsx` (mirroring the `initialMembershipFilter`/`initialQuickFilter` pattern `UserControlPanel` already used), reproducing `RegistrationPanel.jsx`'s own existing `handleSelectEvent` + `setRegisterMemberOpen` pairing rather than inventing new behavior. The self-registration link was relabeled "Register Myself" and moved to the end of the row, left of Archive, now that "Register A Member" exists beside it - on request, to remove the ambiguity between the two. A same-day follow-up, also on request: "Return" from a jumped-to event now goes back to whichever card list actually launched it (Events/Activities, Challenges, Business Listings, or For Sale, tracked as `sourceModule`) instead of into the Registrations module's own event picker, which the admin never intended to browse in the first place; falls back to normal in-module "Return" behavior the moment a filter changes or a different event is picked manually, since at that point the admin is clearly browsing Registrations on its own. The standalone Registrations module was deliberately left in place rather than removed, per an explicit design discussion up front - it still holds cross-event filtering and history lookups that don't belong on one event card; these shortcuts are a faster way into it, not a replacement. |
| 2026-07-28 | Turned Print Registration List's Close button into a Return that jumps back to the source tab. The admin dashboard's tabs live in memory, not the URL, so the print page's history-back fallback landed on the module chooser instead of the Events/Activities (or Registrations) list it was opened from. Print links now carry which tab they came from so Return can jump straight back to it (`EventList.jsx`, `RegistrationPanel.jsx`, `AdminDashboardPage.jsx`, `RegistrationListPrintPage.jsx`). Added `useLocation` to the `react-router-dom` test mock, which the print page now reads for that source-tab state. |
| 2026-07-28 | Fixed a slow Safari print dialog on the registration list print page, in three passes. First attempt: the registration table's scrollable wrapper (`overflow-x: auto`) is slow for Safari to paginate, so it was set to overflow visibly at print time instead - did not fix it. Second attempt: the real bottleneck was `table-layout: auto` making Safari rescan every cell in every column to size them, once per printed page - a known WebKit print-performance issue for large HTML tables (Event and Supply List printing were unaffected because neither has a data table). Fixed layout with explicit column widths did not fix it either. Third attempt, the actual fix: `window.print()` on the live SPA page was the real cause, not the table CSS. Every other report in this app already avoids printing the live page (`EventsPage.jsx`'s event print and `ArchivePanel.jsx`'s archive report build a self-contained HTML document for a popup; `SupplyListViewerPage.jsx` does the same through a hidden same-page iframe). Followed that same hidden-iframe pattern: Print now builds an isolated HTML document (event info, stat pills, registrant table) and prints it through a hidden iframe instead of calling `window.print()` on the live page. The visible page, Return-to-tab behavior, and the direct email-link entry point were unchanged. |
| 2026-07-29 | Connected the Vercel MCP connector (deployment status, build/runtime logs, runtime error aggregation) after confirming with the user it was available and useful, having found no Firebase/Firestore-equivalent connector exists. First real use immediately found something: a recurring `DEP0169` Node deprecation warning for `url.parse()`, logged 564 times over two weeks across `/api/public-registration-counts`, `/api/file-proxy`, and `/api/public-events`. None of those three files call `url.parse()` directly - the actual source is Vercel's own Node runtime, which lazily populates `request.query` using the deprecated API, and only routes that touch `request.query` at all trigger it. Fixed by never touching `request.query`: a new `parseRequestQuery()` helper (`api/_lib/request-query.js`) parses `request.url` with the modern `URL`/`URLSearchParams` API instead, applied to those three routes plus a fourth (`godaddy-supply-list-viewer.js`) with the same pattern that hadn't shown up in the error report yet only because of lower traffic. Verified the replacement matches the old `Array.isArray(value) ? value[0] : value` semantics exactly (encoded values, comma-lists, repeated keys, missing query strings) with a throwaway script before relying on it; full suite green, lint clean. |
| 2026-07-29 | Reworked the two overview docx exports (`APP_OVERVIEW.md`/`ROLE_CAPABILITIES_OVERVIEW.md` rendered to Word) from the project's own hand-rolled OOXML generator (`scripts/export_overview_docx.py`, which only supported flat headings/paragraphs/lists, no table of contents) onto the `docx` npm package instead, on request. New `scripts/export_overview_docx.js` puts the Village Quilters logo and document title into a repeating page header instead of the document body, adds a footer with live page numbers, and generates a real Word-native table of contents from actual heading styles rather than no ToC at all. Verified by round-tripping both exports through Word (via AppleScript, since no reliable local PDF-rendering tool was available) to PDF and checking header/footer repetition, ToC accuracy, and pagination across every page of both documents - not just a visual guess. The old Python script and its now-unneeded dependency were removed. |
| 2026-07-29 | Reset production data for beta testing, on request, ahead of inviting a small group of Guild members to test with a clean slate. Reviewed the actual database with the user before touching anything (a Firebase service account key was generated and added to `.env.local`, gitignored, for this purpose) rather than assuming what was safe to delete: found `events`/`registrations`/`payments`/`squareWebhookEvents`/`auditLogs` were entirely dev/test data (event titles like "Test Challenge", "Witch Hat Wizardry"), but `users` was a mix - a handful of obvious test accounts alongside 25 profiles with real Guild-member names and emails that turned out to match a separate, completely dead `members` collection (zero live callers anywhere in `src/`, confirmed by grep - a leftover from an earlier data model, superseded when CSV import was rebuilt to write `users` directly). Presented the full collection-by-collection plan, ran a dry-run pass showing exactly what would be kept/deleted before touching anything, and got explicit confirmation to delete all non-Super-User profiles (since a CSV re-upload would restore the real members anyway, cleanly exercising the import path). Deleted `members` outright as unused. Kept `appSettings`/`coordinatorAssignments`/`eventLocationDefaults`/`eventTimeDefaults` (real configuration, not test data) and removed one obviously-test location entry ("Kurt'S House" / "Just A Test Address In Here") from the otherwise-real location list. `memberDirectoryProfiles` was resynced to match the surviving Super User rather than deleted directly. Flagged two content gaps found along the way but out of scope to fix here: unfilled placeholder text in `appSettings` (`directoryNote`, the Challenges email instructions) and unconfirmed Square sandbox-vs-live mode. |
| 2026-07-29 | Added a default image for events saved without a photo, on request ("give me a couple of examples" first, then iterated through several rounds of feedback before building the full set). First examples used invented geometric patterns (a pinwheel, a nine-patch) in the app's teal/gold brand colors; asked for "more modern" colors, then a distinct color per type, then - the final direction - real named quilt block patterns rather than invented geometry. Researched the actual piecing structure of four more classic blocks (Ohio Star, Flying Geese, Churn Dash, Bear Paw) and implemented each as SVG polygons matching standard construction, catching and fixing a real bug along the way: the first Flying Geese attempt alternated triangle direction per row, which fused into diamonds instead of reading as geese - real flying geese always point one consistent direction. Final set: Nine Patch (classes, blue), Churn Dash (workshops, coral - "Monkey Wrench" is a literal tool), Ohio Star (retreats, sage), Flying Geese (lectures, plum), Bear Paw (challenges, crimson), Pinwheel (other, mustard). Business Listing and For Sale intentionally excluded, on request. Assets are generated SVGs (`public/assets/event-placeholders/`, regenerated via `scripts/generateEventPlaceholderImages.mjs`) with a bottom-scrim label showing the event type text. Wired into every spot that renders an event thumbnail via a new `getEventPlaceholderImage()` (`src/data/eventOptions.js`): the shared `EventImageCarousel` (and its three call sites), the admin event list, and both print paths - a real uploaded photo always takes priority. 3 new tests on `EventImageCarousel`; full suite green, lint clean. |
| 2026-07-29 | Added a build version tag to the site footer, on request, starting at "Beta 1.0" and bumped by hand for each significant change going forward. New `src/version.js` exports the single `APP_VERSION` string; `App.jsx`'s footer renders it next to the existing copyright/links row. Verified at both desktop and mobile widths - wraps cleanly alongside the existing footer links. |
| 2026-07-29 | Fixed the membership CSV importer silently dropping every row on a real roster upload ("No membership rows were found in the CSV file"). Root cause: the parser always treated row 0 as the header row, but the actual export has a title/banner row above the real header ("Village Quilters Roster July 2026") - not blank (so not already filtered out), just not a header, which meant every column mapped to a garbage key and every row failed the identity check. Fixed by scanning the first several rows for one that actually contains recognizable header tokens (first/last name, email, phone) instead of assuming row 0. Verified against the user's actual uploaded file (251 real rows, previously 0). While fixing this, also wired up a CSV Town/City column - previously present in the roster export but silently ignored - into the profile's `billingAddress.city`, the same field already editable on both the member's own profile page and the admin's `UserControlPanel`. Every import (Add/Update or Annual Refresh) syncs a non-blank Town value; a blank cell leaves the existing city untouched, confirmed with the user as the intended behavior for both modes, including the Annual Refresh path that also inactivates profiles missing from the upload (verified that path already preserved `billingAddress` unconditionally - no bug there, just added test coverage now that it mattered). 15 new tests across CSV parsing and the profile-builder functions; full suite green, lint clean. |
| 2026-07-29 | The CSV fix above immediately surfaced a second, deeper bug: importing the real 251-row roster created every `users` profile and `payments` record successfully, then failed with a delayed "Missing or insufficient permissions" - `memberDirectoryProfiles` stayed at 0. Root cause, confirmed empirically against the Firestore emulator rather than by inference: `memberDirectoryProfiles`' create/update rule reads `users/{id}` via `get()` to decide directory eligibility, but `get()` inside a security rule only sees the database as it stood before the *current* commit - it cannot see a sibling write in the same atomic batch. Four call sites batched a `users` write with its `memberDirectoryProfiles` sync whenever that write made the profile newly eligible (a brand-new active member, or a reactivation): CSV import, the admin's "Add/Edit Membership Profile" form (`saveMembershipProfile`), and both membership/user reactivation flows (`reactivateMembershipProfile`, `reactivateUserProfile`). The directory half of the batch was always refused, and since batches are all-or-nothing, the whole write - profile included - was rejected. (Archiving was unaffected: going ineligible is a directory *delete*, which the rule never gated on eligibility.) Fixed by committing the profile write first and syncing the directory entry as a separate write afterward in all four places. Re-running the fixed code at CSV-import scale surfaced a *third*, unrelated problem: batching 200 directory writes together (fine for the plain profile writes) failed outright - each directory write costs several `get()`/`exists()` calls against the same document, uncached, and Firestore evaluates both the create and update branches of the rule for every `set()`, and 200 blows through Firestore's per-request rule-evaluation budget partway through. Probed the actual safe size empirically (5 succeeds reliably at the full 251-row scale, 10 does not) rather than guessing, and used that for the CSV import's directory-sync chunk size specifically, leaving the 200-per-batch profile-write chunking untouched since that was never the problem. Considered and deliberately did not attempt a broader rules rewrite to cut the redundant `get()` calls at the source (`isSuperUser()`/`isActiveProfile()`/`currentUserData()` each re-fetch the caller's own profile separately) - meaningful but touches core auth helpers used by nearly every rule in the file, too much risk/surface for this fix. New `tests/rules/member-directory-batch-create.test.js` reproduces all three failure modes against the emulator and proves each fix; full suite green (37 rules tests, 261 client, 170 server), lint clean. |
| 2026-07-29 | Split the membership CSV upload into three explicit steps, on request, after the bugs above made the value obvious: choose a file, review a preview, then confirm import - instead of one click doing everything and only surfacing problems after Firestore had already been touched. New `analyzeMemberCsv()` (`ConfigurationPanel.jsx`) parses and validates entirely client-side with zero writes, reporting total/valid row counts and flagging skipped rows (no name/email/phone, by row number) and duplicate emails within the file; `parseMemberCsv()` becomes a thin wrapper over it for backward compatibility. Zero valid rows blocks the import outright with a clear message; warnings don't block, just inform. An explicit "Import N Profiles" button only enabled after review is the only thing that reaches Firestore, using the same chunked/batched writes as before. Also tightened the Membership Profiles card on request ("move the control button side-by-side"): merged two description paragraphs into one, moved the import-result message out of the button row so it can't push controls around, and added a denser action-row spacing class. 8 new tests for `analyzeMemberCsv`; full suite green, lint clean. Live visual verification not performed - would have required signing into the real production admin account for a layout check, judged disproportionate to do without asking first. |
| 2026-07-29 | Reset production membership data a second time, on request ("need to clear out the member profiles!"), to give the now-fixed CSV import and the new segmented upload flow a genuinely clean test. Checked current state first rather than assuming: 252 `users` (251 General User + 1 Super User), 251 `payments`, 1 `memberDirectoryProfiles` entry (evidence a partial re-import or manual profile add had already happened since the first reset). Confirmed scope with the user (same shape as the first reset), then deleted all non-Super-User `users` docs plus all `payments` and all `memberDirectoryProfiles`, verified down to 1/0/0. |
